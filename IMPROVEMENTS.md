# רשימת שיפורים מוצעים — כספת המטבעות
*נכתב 2026-07-03, מבוסס על סקירה של הקוד כפי שהוא היום. מאמץ: S=שעה-שעתיים, M=חצי יום, L=כמה ימים.*

**עדכון 2026-07-04 — בוצעו:** #1 (סנכרון חכם לפי סעיפים), #2 (Service Worker אופליין), #3 (דיפלוי אוטומטי ב-git hook), #4 (גיבוי/שחזור), #5 (מטלות ופעולות לפי ילד), #6 (תמונות אמיתיות בלוח היומי), #7 (קושי מסתגל בחשבון), #8 (חגיגת תג מסך-מלא), #9 (יום חסד לרצף), #12 (דוח שבועי), #13 (Undo למחיקות), #14 (יומן פעולות הורים), #19 (לוג שגיאות במכשיר), #21 (cache headers למשחק).
**עדכון 2026-07-09 — בוצעו גם:** #17 (פיצול index.html ל-index.html/app.js/styles.css, ללא build step, אימות diff שהתוכן זהה בייט-לבייט), #18 (חבילת בדיקות E2E אוטומטיות עם Playwright + הרצה ב-CI על כל push/PR — 12 בדיקות, כולל mutation-test שאישר שהבדיקה על הגנת "מכסה יומית" תפסה עקיפה אמיתית).
**עדכון 2026-07-09 (2) — בוצע גם #16 בגרסה חינמית:** מסתבר שהליבה של "שלט-רחוק הורי חי" לא דרשה FCM/Blaze בכלל — Firebase Realtime Database כבר תומך ב-listener חי (`on('value')`) בתוכנית Spark החינמית, במקום המשיכה החד-פעמית (`once`) שהייתה בשימוש. עכשיו שינוי שהורה אחד עושה מופיע אצל ההורה השני **מיד**, בלי לחכות לרענון/סנכרון ידני. #15 (התראות push אמיתיות כשהאפליקציה סגורה) עדיין דורש FCM+Blaze — לא בוצע.
**עדכון 2026-07-09 (3) — בוצע גם #24 ו-#22:** מצב הדגמה ("נסה בלי הרשמה", טביעת-רגל אפס באחסון) ונכסי חנות (אייקון 512×512 + feature graphic 1024×500, מתארחים ב-`store-assets/`, גם משמשים כ-favicon של האתר).
**הוחלט להשהות:** #23 (תרגום לאנגלית) — הוחלט יחד עם המשתמש לדלג בינתיים; ההיקף האמיתי (הפיכת האפליקציה בפועל לדו-לשונית, כולל מעבר RTL/LTR) הוא עבודה של כמה ימים ולא סוכם על כך.
**נותרו (כולם דורשים Blaze/תשלום, או החלטה שהושהתה):** #15 (התראות push אמיתיות), #20 (פרוקסי Groq), #23 (תרגום, מושהה), #25 (מעבר ל-Blaze עצמו).
*הערה על #11:* ירד מהרשימה — הארנק לא מתרוקן מחוץ למשחק, אז אין מה "לספור" במסך הבית; התצוגה הסטטית נכונה.

## 🔴 הכי מומלץ (השפעה גבוהה, כדאי קודם)

| # | שיפור | למה | מאמץ |
|---|-------|-----|------|
| 1 | **סנכרון חכם במקום "האחרון דורס"** — היום כל דחיפה לענן כותבת את *כל* המשפחה כמקשה אחת. אם שני הורים עורכים במקביל (או ילד מרוויח מטבע בזמן שהורה עורך בטלפון אחר), השינוי של אחד מהם נמחק בשקט. | זו הפינה הכי מסוכנת לאובדן נתונים אמיתי במשפחה עם שני הורים פעילים | M |
| 2 | **עבודה אופליין מלאה (Service Worker)** — האפליקציה נטענת גם בלי אינטרנט, ומסנכרנת כשחוזר חיבור. המשחק ClassiCube כבר אצלנו, גם הוא ייטען אופליין. | ילד בטיסה/במכונית/בבית סבתא בלי WiFi — האפליקציה פשוט לא נפתחת היום | M |
| 3 | **דיפלוי אוטומטי ל-Firebase Hosting** — היום GitHub Pages מתעדכן לבד אבל הכתובת האמיתית (firebaseapp.com) דורשת הרצת `deploy-hosting.ps1` ידנית. שכחה = משתמשים עם גרסה ישנה. | מסיר צעד ידני שבטוח יישכח מתישהו | S |
| 4 | **גיבוי/שחזור ידני** — כפתור "ייצא קובץ גיבוי" ו"שחזר מגיבוי" בהגדרות הורים. | ביטוח נגד כל תקלה — גם למשפחות במצב מקומי בלי ענן בכלל | S |
| 5 | **מטלות לפי ילד** — היום כל המטלות והפעולות משותפות לכל הילדים. לאריאל ולנועה כנראה מגיעות רשימות שונות (גיל שונה, צרכים שונים). | הבסיס להפוך את האפליקציה לרב-ילדית באמת | M |

## 👦 חוויית הילד

| # | שיפור | למה | מאמץ |
|---|-------|-----|------|
| 6 | **תמונות אמיתיות בלוח היומי** — לאפשר להורה לצלם את אריאל מצחצח שיניים ולהצמיד את התמונה למטלה במקום אימוג'י. | פרקטיקה מבוססת-ראיות ללוחות חזותיים לילדים על הרצף: תמונה של הילד עצמו עובדת הכי טוב | M |
| 7 | **רמות קושי מסתגלות בחשבון** — אם הילד פותר מהר ונכון, התרגילים מתקשים בהדרגה (ולהפך). היום הקושי קבוע לפי הגדרת הורה. | שומר על אתגר בלי תסכול; פחות כוונון ידני להורה | M |
| 8 | **חגיגת תג משודרגת** — כשתג חדש נפתח: מסך מלא עם קונפטי ושם התג, לא רק טוסט קטן שנעלם. | הרגע הזה הוא כל הפואנטה של תגים — כרגע קל לפספס אותו | S |
| 9 | **"הקפאת רצף" (Streak Freeze)** — פעם בשבועיים מותר יום אחד "חופש" שלא שובר את הרצף (בהפעלת הורה). | יום חריג אחד ששובר רצף של 25 ימים זה מתכון להתמוטטות רגשית — ומאבד את כל המוטיבציה | S |
| 10 | **צלילי משוב עדינים במסכי הילד** — צליל הצלחה בסימון מטלה בלוח היומי (יש רק במטבעות היום), עם כיבוד מצב רגוע. | חיזוק מיידי עקבי בכל האפליקציה | S |
| 11 | **טיימר "עוד X דקות למשחק" גלוי מהבית** — הבאנר הירוק יציג ספירה חיה כשיש זמן, לא רק כמות סטטית. | שקיפות לילד, פחות "כמה נשאר לי?" | S |

## 👨‍👩‍👧 צד ההורה

| # | שיפור | למה | מאמץ |
|---|-------|-----|------|
| 12 | **דוח שבועי** — מסך (או מייל) שמסכם: כמה מטבעות הורווחו, אילו מטלות הושלמו הכי הרבה/מעט, מצב רצפים, שימוש בכלי רגיעה. | ההורה רואה מגמות במקום נקודות; מזהה מה עובד | M |
| 13 | **ביטול פעולה (Undo)** — מחיקת ילד/מטלה/פרס היום היא סופית ומיידית. טוסט "בוטל ✓ ↩️" ל-10 שניות אחרי כל מחיקה. | לחיצה אחת לא נכונה בטלפון = אובדן נתונים | S |
| 14 | **יומן שינויים הורי (Audit)** — רישום מי (איזה הורה) שינה יתרה/הוסיף פרס/אישר יום נקי, מוצג בהגדרות. | חיוני כששני הורים מנהלים; שקיפות בין בני הזוג | M |
| 15 | **התראת "הילד קנה פרס"** — כשהילד ממיר מטבעות בטלפון שלו, ההורה מקבל push/סימון בטלפון שלו. | סוגר את הלולאה כשההורה לא ליד הילד | L (דורש FCM) |
| 16 | **מסך הורה בטלפון נפרד באמת** — מצב "שלט רחוק": הורה מחובר מהמכשיר שלו רואה ומאשר דברים בזמן אמת (אישור יום נקי מרחוק, זיכוי ידני). היום זה עובד רק דרך סנכרון איטי. | הופך את המולטי-דיווייס ממסונכרן ל"חי" | L |

## 🛠️ תשתית וקוד

| # | שיפור | למה | מאמץ |
|---|-------|-----|------|
| 17 | **פיצול index.html** — הקובץ עבר 4,000 שורות. פיצול ל-app.js/styles.css/index.html (עדיין בלי build step). | עריכה בטוחה יותר, diff-ים קריאים, פחות סיכון לשבירה בטעות | M |
| 18 | **בדיקות אוטומטיות** — סוויטת Playwright קטנה שרצה ב-CI על התרחישים הקריטיים: הרווחת מטבע, קניית פרס, קניית זמן משחק, רצף. | היום כל בדיקה היא ידנית שלי בפריוויו; רגרסיה תתגלה רק אצל אריאל | L |
| 19 | **ניטור שגיאות בפרודקשן** — שליחת שגיאות JS לא-צפויות ל-Firebase (אירוע `errors/` פשוט). | היום שגיאה אצל משתמש אמיתי = בלתי נראית לחלוטין | S |
| 20 | **פרוקסי Groq בצד שרת** — Cloud Function שמחזיקה מפתח אחד משותף עם הגבלת קצב פר-משפחה. (דורש תוכנית Blaze) | חוסם את המכשול האחרון לשיווק המוני; מפתח לא נחשף | L |
| 21 | **דחיסת classicube.js** — הקובץ 1.8MB לא דחוס; Firebase Hosting כבר עושה gzip אבל אפשר לצמצם עוד עם cache headers ארוכים ב-firebase.json. | טעינת משחק מהירה יותר בנתונים סלולריים | S |

## 🚀 לקראת שיווק (מהמסמך הקודם, בקצרה)

| # | שיפור | מאמץ |
|---|-------|------|
| 22 | אייקון + צילומי מסך + feature graphic ל-Play Store (נשאר פתוח מה-checklist) | M |
| 23 | תרגום לאנגלית (i18n) — פתיחת השוק הבינלאומי | L |
| 24 | משפחת הדגמה — "נסה בלי הרשמה" עם נתוני דמו, למי שבוחן את האפליקציה | S |
| 25 | מעבר Firebase ל-Blaze + Budget Alerts (תנאי ל-#15, #20 ולקנה מידה) | S |

---
**ההמלצה שלי לסדר:** 1 → 4 → 3 → 13 → 9 → 2 → 5 → 12. השילוב של (1)+(4) סוגר את סיכוני אובדן-הנתונים לפני שמזמינים עוד משפחות; (9)+(13) הם הרווחים המהירים הכי מורגשים ביום-יום.
