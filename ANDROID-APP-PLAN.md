**עדכון 2026-07-22 (6) — השורש האמיתי (אבחון על המכשיר דרך ADB): Family Link חוסם את שירות הנגישות. מעבר לארכיטקטורת Device Owner:**
כל התיקונים (3)-(5) שיפרו קוד ש**מעולם לא רץ**. אבחון על המכשיר (Mi 10T, Android 12) דרך ADB חשף:
`dumpsys accessibility` הראה `Crashed services:{...GameTimeAccessibilityService}`, ו-`accessibility_enabled=0`
למרות שהמשתמש הפעיל את השירות; בקריאה שנייה `enabled_accessibility_services=null` — **מישהו כיבה אותו**.
`dumpsys device_policy` הראה `Profile Owner (User 0): com.google.android.gms` (זה Family Link) עם
`accessibility services: empty` — רשימת-היתר ריקה = **כל שירותי הנגישות של צד-שלישי חסומים במדיניות**.
זו הגנה מכוונת של Family Link (שירות נגישות = הדרך הקלאסית לעקוף בקרת הורים). **מסקנה:** שירות נגישות
וכספת המטבעות לא יכולים לחיות יחד עם Family Link על אותו פרופיל — Family Link מנצח כ-Profile Owner.
זה הסביר את **שתי** התלונות בבת אחת: החסימה לא עבדה (השירות מת) והטיימר לא נעצר (ה-overlay שאל שירות מת
וקיבל null). **הפתרון (בחירת המשתמש): Device Owner.** במקום שירות נגישות, כספת המטבעות מוקצית כ-device
owner (`adb shell dpm set-device-owner com.coinquest.app.debug/com.coinquest.app.CoinQuestDeviceAdminReceiver`
על מכשיר מאופס בלי חשבונות) ומשתמשת ב-`DevicePolicyManager.setPackagesSuspended` כדי **להשהות** את
מיינקראפט כברירת מחדל — אפליקציה מושהית לא ניתנת להפעלה כלל (חסימה ברמת ה-OS, ש-Family Link לא נוכח
בכלל כדי לחסום, כי אין פרופיל מפוקח). זרימה: ברירת מחדל = מיינקראפט מושהה; קניית זמן → `allow()` משחרר
לפני הפעלה; סיום סשן (timeout/✖/עזיבה) → `block()` משהה שוב (זה גם ה-hard-stop של "נגמר הזמן" — המערכת
מעיפה את הילד מהמשחק). **קבצים חדשים:** `CoinQuestDeviceAdminReceiver.kt`, `GamePolicyManager.kt`,
`res/xml/device_admin.xml`. **שונו:** manifest (receiver של device-admin; הוסר ב-play flavor),
`NativeGameBridge` (`isDeviceOwner()`, `blockGames()`, gating על device-owner במקום נגישות, `allow()` לפני הפעלה),
`GameTimeOverlayService` (re-suspend בסיום; guard מפני overlay כפול — הבאג של "שני מונים"), `app.js`
(gating + אזהרת אכיפה + הודעות על device-owner). שירות הנגישות נשאר **אופציונלי** לזיהוי-עזיבה מוקדם
בלבד (עובד עכשיו כי אין Family Link), אך אינו נדרש להתחלת סשן. *אימות:* `assembleFamilyDebug` הצליח,
30/30 Playwright עוברים. **דורש: איפוס מכשיר להגדרות יצרן, דילוג על חשבון Google, הקצאת device owner
דרך ADB, ואז בדיקה בפועל.**

**עדכון 2026-07-21 (5) — הטיימר עדיין לא נעצר (Samsung/One UI): מעבר לשאילתה אקטיבית + חזרה לאפליקציה במקום מסך הבית:**
הפולינג מ-(4) עדיין הסתמך על `foregroundPackage` שמתעדכן רק מאירועי `TYPE_WINDOW_STATE_CHANGED` —
ועל מכשירי Samsung/One UI (המכשיר של המשתמש) אירוע העזיבה (Home/Back) פשוט **לא נורה באופן אמין**
(הפעלת אפליקציה תמיד יורה אירוע — לכן חסימת הכניסה-מחדש עובדת — אבל היציאה נבלעת). **התיקון:**
במקום לחכות לאירוע, ה-overlay שואל עכשיו בכל טיק **אקטיבית** מי האפליקציה בחזית כרגע דרך
`rootInActiveWindow?.packageName` (נדרש `canRetrieveWindowContent="true"` ב-config — שונה מ-false).
מסך הבית של המערכת ומעבר לאפליקציה אחרת תמיד ניתנים לשאילתה, כך שהיציאה נתפסת גם כשלא נורה אירוע.
נשמר fallback ל-`foregroundPackage` המבוסס-אירועים אם השאילתה מחזירה null (למשל משחק SurfaceView טהור
שאין לו עץ נגישות בזמן שהוא בחזית — ה-fallback קורא שם את המשחק). **בעיה שנייה שדווחה — לחיצה על
ה-✖ סגרה גם את כספת המטבעות:** endSession כפה `GLOBAL_ACTION_HOME` (מסך הבית של הטלפון), מה שנתפס
כ"האפליקציה נסגרה". שונה: בסיום דרך timeout או ✖, מביאים את `MainActivity` של כספת המטבעות לחזית
(`bringCoinQuestToFront`, מותר מ-service בזכות הרשאת ה-overlay שממילא נדרשת) — הילד נוחת בחזרה
באפליקציה ורואה את המטבעות/הזמן המעודכנים. יציאה עצמאית (Home/מעבר לאפליקציה) לא מושכת את כספת
המטבעות קדימה (`returnToApp=false`). `goHome()` הישן הוסר (חסימת הכניסה-מחדש עדיין קוראת ישירות
ל-`performGlobalAction(GLOBAL_ACTION_HOME)`, לא הושפעה). **חשוב להתקנה:** מכיוון ש-`canRetrieveWindowContent`
השתנה, יש **לכבות ולהדליק מחדש את שירות הנגישות** של כספת המטבעות אחרי התקנת ה-APK (הגדרות →
נגישות → כספת המטבעות → כבוי/פעיל), אחרת ההרשאה החדשה לקריאת החלון הפעיל לא תיכנס לתוקף ו-`rootInActiveWindow`
יחזיר null. *אימות:* `assembleFamilyDebug` הצליח; **דורש בדיקה בפועל על המכשיר**.

**עדכון 2026-07-21 (4) — תיקון עצירת הטיימר הנייטיבי: מעבר משיטת "אירוע עזיבה" לפולינג:**
הגישה מ-(3) — שירות הנגישות מזהה אירוע חלון של אפליקציה שאינה-היעד ומתזמן סיום — **לא עבדה בפועל
על המכשיר**: הילד יצא ממיינקראפט והטיימר המשיך לרוץ עד שלחצו על ה-✖ שלו. הסיבה הסבירה: הסתמכות
על תפיסת אירוע-המעבר הבודד ("אפליקציה אחרת עלתה קדימה") שברירית — על חלק מהמכשירים/ניווט-מחוות
הוא פשוט לא נורה בצורה שתפסנו. **הפתרון החדש (חסין יותר):** במקום לחכות שיודיעו לנו, ה-`CountDownTimer`
של החלון הצף בודק **בכל טיק (כל שנייה)** מהי אפליקציית ה-foreground הנוכחית. שירות הנגישות מתחזק
עכשיו שדה סטטי `foregroundPackage` (מעודכן בכל שינוי חלון, למעט חבילת האפליקציה שלנו עצמה, כדי
שהחלון-הצף לא ייחשב בטעות כ"עזיבת המשחק"), וה-overlay מסיים את הסשן ברגע שה-foreground אינו
המשחק במשך יותר מ-3 שניות (`AWAY_GRACE_MS`, סבילות להצצה מהירה בווילון ההתראות). הזרוע רק
**אחרי** שהמשחק היה foreground לפחות פעם אחת (`seenTargetForeground`), כדי שחלון-הזמן של ~שנייה
בין הפעלת המשחק להופעת החלון הראשון שלו לא יסיים את הסשן מיד בלידתו. ההיגיון עבר כולו ל-overlay
עצמו (הוא מסיים את עצמו), כך שאין יותר תלות ב-handoff סטטי accessibility→overlay. שיטת ה-scheduleAwayEnd
הישנה + `GameTimeOverlayService.instance` הוסרו. *אימות:* `assembleFamilyDebug` הצליח; **דורש
בדיקה בפועל על מכשיר** (Kotlin בלבד): לקנות זמן, להיכנס למיינקראפט, ללחוץ Home/לצאת, ולוודא שהחלון
הצף/הטיימר נעלמים תוך ~3-4 שניות מבלי ללחוץ על ה-✖.

**עדכון 2026-07-21 (3) — שני באגים מבדיקת מכשיר: אין איקון ברור למשחקים + הטיימר הנייטיבי לא נעצר ביציאה:**

**1) אין איקון ברור להגעה לרשימת המשחקים:** מסך הבית הראשי ("⚡ מה עושים?") הכיל 5 כפתורים
גדולים (סריקה/חשבון/פרסים/יומן/מכרה הידע) אבל **בלי** כפתור למשחקים — הדרך היחידה להגיע
לרשימת המשחקים הייתה באנר דינמי שמופיע נמוך יותר במסך (`gameTimeWrap`), בתוך שלל באנרים
אחרים (אירועים, רצף, תגים), קל לפספס. נוסף כפתור שישי לרשת הראשית: "🎮 המשחקים שלי" (צבע
כחול חדש, `--sky`), עם אותה עקביות ויזואלית כמו שאר הכפתורים. הבאנר הדינמי הישן נשאר (הוא
מציג מידע חי — כמה זמן נשאר), שני נתיבי גישה עכשיו קיימים.

**2) הטיימר הצף (מונה למעלה) לא נעצר כשיוצאים מהמשחק בפועל:** `GameTimeOverlayService` (החלון
הצף שמראה את הספירה לאחור מעל מיינקראפט) פשוט המשיך לספור עד תום הזמן שנקנה, **גם אם הילד
יצא מהמשחק** (Home/Back) לגמרי — כי הוא לא ידע שהילד עזב; רק לחיצה מפורשת על ה-✖ בחלון הצף
עצרה אותו. `GameTimeAccessibilityService` כבר מקבל אירועי חלון עבור **כל** אפליקציה (לא רק
המשחק הנאכף, ראו `game_time_accessibility_config.xml`), אז נוסף זיהוי כיוון הפוך: כשחלון
של אפליקציה **שאינה** יעד הסשן הפעיל עולה קדימה, נפתח טיימר-חסד של 4 שניות (`AWAY_GRACE_MS`)
— אם הילד לא חוזר למשחק תוך זמן זה, הסשן מסתיים מוקדם (מדווח בדיוק את הזמן שנוצל, מרענן את
הארנק בהתאם), **בלי** לכפות "בית" (אם הילד עבר במכוון לאפליקציה/שיחה אחרת, כפיית GLOBAL_ACTION_HOME
הייתה מוציאה אותו ממנה בכוח — לא רצוי). חזרה למשחק תוך 4 השניות מבטלת את הטיימר (false alarm —
למשל הצצה מהירה בשעה או וילון התראות). נוסף `GameTimeOverlayService.instance`
(`endEarlyDueToAppSwitch()`) ו-`endSession()` קיבלה פרמטר `forceHome` (true עבור timeout/סגירה
ידנית כרגיל, false עבור העזיבה-מהמשחק). *אימות:* `assembleFamilyDebug` הצליח; 30/30
Playwright עוברות (שינוי זה הוא Kotlin בלבד, ללא נגיעה ב-JS/tests); **לא נבדק עדיין בפועל על
מכשיר אמיתי** — דורש APK חדש + בדיקה: לקנות זמן משחק, להיכנס למיינקראפט, ללחוץ Home, ולוודא
שהטיימר/החלון הצף נעלמים תוך כמה שניות במקום להמשיך לרוץ.

**עדכון 2026-07-21 (2) — מטלות "צ'ק-בוקס" עוברות מדיווח-עצמי לאימות בסריקת QR:**
בקשת ההורה: ילד יכול היה לסמן שביצע מטלה (צחצוח שיניים וכו') בלחיצה פשוטה על מסך הבית, בלי שום
אימות שהמטלה אכן בוצעה — לחיצה = מטבעות, מיד. תוקן: לחיצה על תיבת הסימון של מטלה כבר **לא**
מזכה מטבעות בעצמה — היא לוקחת את הילד ישר למסך הסורק, עם רמז מדויק על גבי המסך ("סרוק את קוד
ה-QR של: <שם המטלה>"), כך שרק סריקה אמיתית של הקוד שההורה יצר והדביק במקום הרלוונטי (למשל
מדבקה ליד הכיור) מזכה בפועל. הפונקציה הישנה `markChore()` (שהעניקה קרדיט ישירות מהלחיצה)
הוסרה לגמרי — `redeemToken()` (הפונקציה שכבר משרתת את הסריקה/הקלדה הידנית, עם כל בדיקות
האנטי-זיוף/מקסימום-יומי/קירור-60-שניות הקיימות) היא כעת המקור היחיד לזיכוי מטלות. נוסף
`goScanForChore(id)` (מנווט לסורק + שומר את זהות המטלה) ו-`applyScanIntentHint()`/`openScan()`
(מציגים/מנקים את הרמז המותאם למטלה, כדי שהכניסה הגנרית לסורק מהתפריט התחתון לא תישאר עם רמז
ישן ממטלה קודמת). *אימות:* עודכנו/נוספו בדיקות Playwright (כולל בדיקה חדשה שמוודאת שלחיצה בלבד
לא מזכה כלום ופותחת את הסורק עם הרמז הנכון) — 30/30 עוברות; אומת גם ידנית בדפדפן (Browser pane)
כולל החזרת הרמז לגנרי בכניסה דרך הכפתור/תפריט התחתון הרגיל.

**עדכון 2026-07-21 — Google Sign-In אושר עובד על מכשיר אמיתי + סבב תיקוני באגים ושיפורים כלל-אפליקתיים:**

**סגירת שרשרת בדיקת המכשיר (המשך ל-2026-07-20 (5)):** אחרי מילוי ה-`WEB_CLIENT_ID` האמיתי
עדיין נתקלנו ב-`DEVELOPER_ERROR` (קוד 10) — התברר שה-debug build האמיתי מותקן תחת
`com.coinquest.app.debug` (בגלל `applicationIdSuffix ".debug"` ב-build type debug), בזמן
שב-Firebase נרשם רק `com.coinquest.app` (בלי הסיומת) עם ה-SHA-1. נדרשה רישום אפליקציית
Android **שנייה** ב-Firebase עבור `com.coinquest.app.debug` עם אותו SHA-1. **אושר עובד
בפועל על מכשיר אמיתי.** בהמשך נתקלנו גם בחסימת "Restricted Settings" של אנדרואיד 13+
שמנעה הפעלת שירות הנגישות ל-APK שהותקן מ-sideload — נפתר דרך Settings → Apps → כספת
המטבעות → תפריט ⋮ → "Allow restricted settings".

**סבב סקירת קוד + תיקוני באגים (ללא בקשה ספציפית — ביקורת יזומה):**
- **תאריכי אירועים שבורים:** `todayDateStr()` החזירה תאריך לא-מרופד (`2026-7-5`) בעוד
  `<input type="date">` תמיד מחזיר מרופד (`2026-07-05`) — כל השוואת `e.date===today`
  נכשלה תמיד. שום אירוע עם תאריך שנבחר לא הופיע/הזכיר. תוקן + `normEvDate()` שמנרמל גם
  ערכים ישנים שנשמרו לא-מרופדים.
- **שער למידה (learning gate) שבור לתשובות טקסט:** `onclick="answerGateQuestion(${JSON.stringify(c)})"`
  יצר גרשיים מקוננים בתוך attribute מגורש בכפולים — כל תשובת אנגלית/מדעים בשער הייתה
  שבורה (רק תשובות מספריות עבדו במקרה). נבנה מחדש עם `createElement`+`.onclick`.
  אותה בעיה בדיוק תוקנה בעבר ב-`renderLearningQuestion` הרגיל; השער עצמו פוספס.
- **סריקת QR עוקפת קירור 60 שניות:** `markChore` אכף `CHORE_MIN_GAP_MS` בין סימונים אבל
  `redeemToken` (אותה מטלה, דרך סריקה) לא — אפשר היה לסרוק את אותו קוד שוב ושוב ולמלא
  מכסת יום בשניות. תוקן עם אותו guard.
- **דליפת/אובדן נתוני "מכרה הידע":** `cs_learn_`/`cs_learnlvl_` חסרו ברשימות מחיקת ילד,
  ניקוי-בין-חשבונות (`clearLocalFamilyData` — יחד עם `cs_learning`/`cs_auditlog`/`cs_pin`
  שגם דלפו בין משפחות על מכשיר משותף), וגיבוי/שחזור.
- **דמות "סטיב" ממיינקראפט** נשארה קשיחה למסך בחירת הפרופיל (`id==='ariel'`) בפלטה
  המדויקת של Steve כולל חרב — בדיוק סיכון הקניין הרוחני ש-G1 טיפל בו בכל שאר המסכים.
  הוחלף ב-`blockyAvatarSvg` הגנרי לפי `childTheme()`.
- **מתגי אדמין לא נשמרים:** `toggleLearningEnabled`/`toggleMathEnabled` וכו' שינו רק
  state בזיכרון — הורה שיצא בלי "שמור" איבד את השינוי ברענון. נשמרים עכשיו מיד.
- שורה מתה ב-`recomputeStreak` (`if(s.current<s.goal) s.wonAt=s.wonAt;`) הוחלפה בהערה.

**תיקוני אכיפה נייטיבית (Kotlin + JS, דורש APK חדש):**
- **זמן משחק לא מחויב אם התהליך נהרג באמצע משחק:** `GameTimeOverlayService` כתב את הזמן
  שנצרך רק בדיווח סופי ל-WebView חי — קריסה/הריגת תהליך = הילד "מקבל את הזמן בחזרה".
  עכשיו כל טיק כותב ל-SharedPreferences (`CONSUMED_PENDING_SECONDS`); בעלייה הבאה
  `recoverPendingNativeConsume()` מחייב את מה שלא דווח ומנקה.
- **חיוב ילד שגוי:** אם הוחלף פרופיל בזמן שהמשחק הנייטיבי רץ ברקע, האח/ות היו מחויבים.
  `childId` מועבר עכשיו עם `startNativeSession` וחוזר בקולבק (`SESSION_CHILD_ID`).
- דליפת `NativeGameBridge.instance` הסטטי נסגרה ב-`onDestroy`.

**שיפורים כלל-אפליקתיים:**
- **התראות אירועים נייטיביות (החדש הגדול ביותר):** עד עכשיו תזכורת אירוע נורתה רק כשהאפליקציה
  פתוחה על המסך (`checkEventReminders` פולינג בתוך ה-JS) — חסר שימוש לתכלית התזכורת. נבנה
  `EventReminderScheduler.kt` חדש: מקבל מ-`app.js` (גשר `syncEventReminders`) רשימת JSON של
  כל תזכורת עתידית, מזרים/מבטל אזעקות `AlarmManager` בהתאם (diff מול הרשימה הקודמת),
  ו-`ReminderReceiver` מציג התראה אמיתית (ערוץ `IMPORTANCE_HIGH`, פר-אירוע) ומזרים מחדש
  אחרי reboot. נקרא מ-app.js בעלייה, אחרי הוספה/מחיקה מקומית, ואחרי כל סנכרון ענן.
- **צ'אט "איזי":** `speakText` מעדיף עכשיו TTS נייטיבי (AN1) — קול עברית ב-WebView לרוב
  פשוט לא מותקן, אז הצ'אט היה בפועל אילם; ותוספת בדיקת `response.ok` לפני `res.json()`
  כדי שלא תיזרק שגיאת JSON גולמית מבהילה על מפתח פגום/rate-limit.
- **נעילת PIN בפני ניחוש** (`_pinLockUntil`) נשמרת עכשיו ב-localStorage — רענון דף כבר לא
  מאפס את הקירור אחרי 5 ניסיונות כושלים.
- **Service Worker:** `app.js`/`questions.js`/`styles.css` (לוגיקה, לא נכסים דקורטיביים)
  היו stale-while-revalidate — כל דיפלוי היה דורש טעינה אחת "מבוזבזת" עם הקוד הישן לפני
  שהתיקון מופיע. שונה ל-network-first (כמו ניווטים), עם אותו fallback ל-cache אופליין.
  `CACHE_VERSION` עלה ל-`cq-v6`.
- הוספת `min=todayDateStr()` לשדה תאריך אירוע — לא ניתן יותר לבחור בטעות תאריך שכבר עבר.

*אימות:* `assembleFamilyDebug` הצליח אחרי כל שינויי ה-Kotlin; 29/29 בדיקות Playwright
עוברות (single-worker). תיקוני האכיפה הנייטיבית והתראות האירועים דורשים בדיקה בפועל על
מכשיר (לא ניתן לאמת ב-Playwright/דפדפן) — עדיין לא נבדקו על מכשיר אמיתי.

---

**עדכון 2026-07-20 (5) — נמצא ותוקן באג אמיתי במכשיר: Google Sign-In לא עבד ב-APK:**
בבדיקה הראשונה על מכשיר אמיתי (build ה-family), לחיצה על "התחברות עם Google" נכשלה עם
`Error 403: disallowed_useragent`. **הסיבה:** גוגל חוסמת לצמיתות (מדיניות אנטי-פישינג
מ-2016, לא ניתן לעקוף עם spoofing של user agent) כל זרימת OAuth שמתבצעת בתוך WebView
כלשהו — וכל האפליקציה רצה בתוך WebView. הפתרון הנכון: התחברות **נייטיבית** ב-Kotlin
(Google Play Services), ואז העברת ה-ID token שמתקבל ל-Firebase JS SDK דרך
`signInWithCredential` (במקום `signInWithPopup`/`signInWithRedirect` שדורשים OAuth
בתוך ה-WebView עצמו).

**מה נבנה:**
- `build.gradle`: תלות חדשה `com.google.android.gms:play-services-auth`.
- `MainActivity.kt`: `GoogleSignInClient` + `ActivityResultLauncher` שמריץ את בורר
  החשבונות האמיתי של Play Services (לא עמוד web), מחלץ `idToken`, ומעביר אותו ל-JS
  דרך `webView.evaluateJavascript(...window.onNativeGoogleSignIn(idToken)...)`. ביטול
  התחברות/שגיאה מדווחים דרך `window.onNativeGoogleSignInError`. מנקה session קיים
  (`signOut()`) לפני כל ניסיון כדי שמכשיר משפחתי משותף לא ינחש איזה הורה מחובר.
- `NativeGameBridge.kt`: מתודת גשר חדשה `nativeGoogleSignIn()`.
- `app.js`: `signInWithGoogle()` בודקת אם קיים גשר נייטיבי ומעדיפה אותו; שתי פונקציות
  callback חדשות (`onNativeGoogleSignIn`/`onNativeGoogleSignInError`) שמשלימות את
  ההתחברות דרך `firebase.auth.GoogleAuthProvider.credential(idToken)` +
  `signInWithCredential` — משם ואילך זה בדיוק אותו נתיב (`onAuthStateChanged` →
  `handleSignedInUser`) שכבר קיים ועובד לזרימות הדפדפן.

**פעולה נדרשת מהמשתמש — חובה לפני שזה יעבוד בפועל:** ב-`MainActivity.kt` יש קבוע
`WEB_CLIENT_ID = "REPLACE_WITH_FIREBASE_WEB_CLIENT_ID"` — placeholder בלבד. חייבים
להחליף אותו ב-Web Client ID האמיתי (מחרוזת שמסתיימת ב-`.apps.googleusercontent.com`),
נמצא ב-Firebase Console → Authentication → Sign-in method → Google → Web SDK
configuration → Web client ID (או ב-Google Cloud Console → APIs & Services →
Credentials → "Web client (auto created by Google Service)"). זה **לא** אחד השדות
הקיימים ב-`firebaseConfig` בתוך `app.js` — מזהה OAuth נפרד לגמרי.

*אימות:* קומפילציה מלאה של flavor ה-family; חיווט ה-JS אומת בדפדפן עם גשר מדומה
(הקריאה לגשר, הודעות סטטוס בעת פתיחה/ביטול/שגיאה) — **לא ניתן לאמת את חלק ה-Kotlin
בפועל בלי מכשיר אמיתי + ה-Web Client ID הנכון**, וזה עדיין לא נבדק על מכשיר לאחר
התיקון. 29/29 בדיקות Playwright עוברות (הנתיב הנייטיבי לא נכנס לפעולה בבדיקות
מקומיות, בלי גשר).

**עדכון 2026-07-20 (4) — בוצעו G3, G4, G5, G7, G8 + ביקורות A7/A8 (סיום חלקים ב'+ג'):**

*G3 (מד חיסכון):* היה קיים כבר פס התקדמות לינארי דק (8px) עם "X / Y מטבעות" בטקסט —
**שודרג** ולא נבנה מאפס: הוגבה ל-14px, נוסף מספר אחוזים מפורש, וזוהר זהוב עדין כש-70%+
(concrete וברור יותר לילד בן 7 בלי לבנות עיגול SVG חדש).

*G4 (אווטאר מטבע מתפתח):* לא היה קיים בכלל — יתרה הוצגה כמספר יבש בלבד. **נבנה**: 5
שלבים ויזואליים (🪙 בודדים → 👛 כיס → 💰 שקית → 📦 תיבה → 🏦 כספת זוהרת ב-200+), מוחלפים
בתוך אותו עיגול זהב קיים בגיבור המסך (`#bigCoinIcon`), בלי לשנות את אנימציית ה-bob
הקיימת. הכספת הזוהרת (200+) היא רפרנס ישיר לשם האפליקציה עצמה.

*G5 (חגיגות מדורגות) — ביקורת + תיקון אמיתי:* המנגנון התלת-שכבתי כבר קיים ותקין (תשובה
רגילה = coinFly קטן; badge = מודל חגיגה מלא + קונפטי; אף פעם לא הבזקים/רעד/צליל חד).
**נמצא פער אחד:** מפגש למידה מושלם (השכבה האמצעית) קיבל צליל גדול יותר אך **אפס אפקט
חזותי** — תוקן בהוספת `coinBurst()` (קונפטי) לצד ה-chime הקיים.

*G7 (רקעים לפי שעת יום):** לא היה קיים כלל לילדים בלי theme (מיינקראפט/חד-קרן כבר יש
להם day/night בינארי משלהם, ב-!important, שמנצח כשקיים). **נבנה**: 4 גרדיאנטים
(`period-morning/afternoon/evening/sleep`) על `.app`, מבוססים על `currentPeriodKey()`
הקיים. **תוקן במהלך בדיקה:** גרסת "לילה" הראשונית הייתה נאבי כהה אמיתי — הוחלפה לטון
בהיר-קריר ("דמדומים") כי לטקסט הכהה הקיים (section-title וכו') אין override של טקסט
בהיר לרקע כהה, וזה היה שובר קריאות. **נמצא ותוקן באג אמיתי נוסף:** התנאי המקורי
(`!cur()`) לא ניקה את המחלקה במסך הבורר (picker) כי `cur()` ממשיך להחזיר את הילד
האחרון גם כשרק *מציגים* את הבורר — תוקן לבדיקה לפי `currentView` כמו המסקוט.

*G8 (רטט עדין):* לא היה קיים כלל. **נוסף:** `navigator.vibrate(20)` בכל `addPoints`
חיובי (מטלה/תשובה נכונה), מדלג במצב רגוע. לא נוסף מתג הגדרות נפרד — מצב רגוע + בקרת
המכשיר עצמו (שכבר מאפשרת כיבוי רטט גלובלי) נחשבו כיסוי מספיק.

*A7 (מסכי-מעבר) — ביקורת:* התהליך הקיים (מסך סיכום מפגש שדורש הקשה להמשך, ו-10 שניות
"היה כיף" נייטיביות עם כפתור סגירה מוסתר) **כבר עונה על העיקרון** (בלי ניתוק פתאומי) —
ולמעשה עדיף על טיימר-קבוע אוטומטי כי הילד קובע את הקצב. לא נוספה שכבת מעבר נוספת.

*A8 (מילון סמלים עקבי) — ביקורת:* נבדקו 🪙/⏳/✅/🎁 בכל הקוד — עקביים לגמרי, אין סמל
שמשמש לשני מובנים. אין פערים.

*אימות:* 29/29 בדיקות Playwright; כל תכונה אומתה ידנית בדפדפן (כולל תיקוני הבאגים
שנמצאו תוך כדי הבדיקה עצמה — G5, G7 פעמיים).

**עדכון 2026-07-20 (3) — בוצעו A3-A5, G1, G2, G6 (המשך חלקים ב'+ג'):**

*A3 (סדר קבוע):* ביקורת מצאה שהעיקרון עצמו כבר מיושם נכון (state.chores הוא סדר-הכנסה
קבוע, מטלה שהושלמה מקבלת ✓ ונשארת במקום, אין מיון אוטומטי) — וגם ש**משימות מתוזמנות
(anchored) כבר יש להן drag-to-reorder מלא ומתקדם** (Pointer Events, עובד גם במגע).
הפער היחיד: רשימות המטלות/פעולות הרגילות (`renderChoresAdmin`/`renderActionsAdmin`)
לא היו ניתנות לסידור-מחדש בלי מחיקה+יצירה. **תוקן:** מנגנון גנרי `startListDrag`
שמכליל את אותה לוגיקה בלי לשכתב את הגרסה המוכחת של anchored, מחובר לשתי הרשימות.

*A4 (אפס ענישה):* ביקורת מלאה — הנתיב היחיד להורדת מטבעות הוא קנייה מודעת של הילד
(redeemReward). תשובה שגויה בלמידה: צבע קורל רגוע (לא אדום מבהיל), בלי X, בלי צליל
שלילי בשום מקום באפליקציה. **אין פערים** — התהליך תקין כמו שהוא.

*A5 (הסתגלות קושי שקטה):* נמצא פער אמיתי — עליית רמה (גם בלמידה וגם בחשבון) הכריזה
לילד ב-toast ("עלית לרמה..."), בזמן שירידת רמה הייתה שקטה. **תוקן:** שני המנגנונים
(`bumpLearningLevel`, `bumpMathLevel`) שקטים כעת בשני הכיוונים — ההורה רואה את הרמה
בהגדרות, הילד לא.

*G6 (אייקוני מטלות מרשימה סגורה):* נוסף בורר אימוג'י אוצים (20 סמלים) בטפסי "הוסף
מטלה/פעולה" בהגדרות ההורה — לחיצה ממלאה את השדה הקיים (עדיין free-text לגמישות).
עיגול הסימון במסך הילד הוגדל מ-46px ל-52px עם גופן גדול יותר, לזיהוי קל יותר.

*G1 (מסקוט מלווה) — כולל תיקון פער S3 שלא התגלה קודם:* בזמן בניית המסקוט התגלה
ש-`renderBalance()` כלל SVG פיקסלי קבוע (avatar של אריאל) שצבעיו הם **ממש הפלטה של
Steve** — ראש בצבע עור, עיניים כחולות קטנות, גוף cyan, רגליים כחולות-סגולות — סיכון
דמיון-דמות אמיתי שהניקוי הקודם (S3) לא תפס כי הוא חיפש רק את המילה "Minecraft" בטקסט,
לא בדק מה ה-SVG הזה בפועל מצייר. **תוקן:** דמות בלוקית גנרית חדשה (`blockyAvatarSvg`)
בצבע **של הילד עצמו** (לא צבעי עור/cyan קבועים), עם עיניים גדולות ועגולות וחיוך —
שונה בכל ציר שהפך את המקור לזיהוי. משמשת גם כאווטאר (כמו קודם) וגם כמסקוט המגיב:
בועה צפה מעל התפריט התחתון (`mascotWrap`), שמגיבה (קפיצה + פה פתוח לרגע) לכל מטבע
שמתקבל דרך `addPoints` — לא לרכישות. מכבד calm mode ו-prefers-reduced-motion.

*G2 (מפת מסע יומית):* רכיב חדש (`journeyMapWrap`, לא מיזוג עם ה-day-strip הקיים שממשיך
לשמש לאוריינטציה בזמן-יום — תפקיד שונה) — תחנה לכל מטלה של היום (מכל התקופות, לא רק
הנוכחית), נצבעת בסיום, אווטאר הילד יושב "לפי כמות שהושלמה" (לא קשור לזהות המטלה
הספציפית, כדי שסדר ביצוע חופשי לא ישבור את התצוגה), תיבת אוצר שנפתחת בסיום כל המטלות.
**באג אמיתי שנמצא ותוקן תוך כדי בדיקה ידנית:** הרכיב לא התרוקן במעבר למסך אחר (היה
מחובר רק בתוך `renderChores()`, שרץ רק במסך הבית) — נשאר גלוי עם תוכן ישן במסכים
אחרים. תוקן בקריאה ישירה מ-`go()`, כמו המסקוט והרצועה של A1.

*אימות:* 29/29 בדיקות Playwright; כל התכונות אומתו ידנית בדפדפן כולל המקרים הגבוליים
(מצב ריק, מצב מלא, מעבר בין מסכים, מצב רגוע).

**עדכון 2026-07-20 (2) — ביקורת מנגנון מיינקראפט + סגירת שני פערי אכיפה קריטיים:**

*שרשרת ההפעלה (נבדקה חוליה-חוליה, תקינה):* קניית דקות → `k.gtime` → מסך משחקים →
`beginGameLaunch` → `startNativeGameSession` (בודק גשר/התקנה/הרשאות, מסרב אם `gtime<=0`)
→ `NativeGameBridge.startNativeSession` (מסרב להתחיל סשן שאי אפשר לאכוף) → שירות
overlay עם ספירה לאחור → בסיום: כפיית חזרה למסך הבית + דיווח שניות אמיתיות חזרה ל-JS
שמחייב את הארנק (`onNativeGameSessionEnded` — מקור האמת היחיד לזמן שנוצל).

*שני פערים קריטיים שנמצאו — שניהם שברו את הדרישה "בלי קניית זמן אין משחק" — ותוקנו:*
1. **החומה קמה רק אחרי הסשן הראשון אי-פעם:** `TARGET_PACKAGE` נכתב רק כשסשן מתחיל,
   אז בהתקנה טרייה שירות הנגישות לא ידע מה לחסום והילד יכל לפתוח מיינקראפט חופשי בלי
   לקנות כלום. **תוקן:** pref חדש `ENFORCED_PACKAGES` (CSV) שמוזרע מ-`app.js` בכל
   הפעלת אפליקציה (`applyEnforcedPackages()`, גם בכל כניסה למסך המשחקים לתפיסת
   משחקים שנוספו/הוסרו) דרך מתודת גשר חדשה `setEnforcedPackages()`; שירות הנגישות חוסם
   כל חבילה בסט (עם fallback ל-`TARGET_PACKAGE` הישן לתאימות אחורה).
2. **סשן שקרס = משחק חופשי לנצח:** אם תהליך ה-overlay נהרג באמצע סשן (קריסה/OOM/
   force-stop), `SESSION_ACTIVE` נשאר `true` לתמיד ושירות הנגישות הרשה הכל. **תוקן:**
   pref חדש `SESSION_END_AT` (דדליין שעון-קיר שנכתב עם תחילת כל סשן, נמחק בסיומו התקין);
   שירות הנגישות מחשיב סשן כתקף רק אם הדגל פעיל **וגם** הדדליין לא עבר (+30 שניות חסד,
   שמכסות גם את מסך ה"היה כיף" של 10 השניות), ומרפא-עצמית דגל תקוע.
3. **בונוס — נראות להורה:** מתג הנגישות יושב בהגדרות המכשיר, בלי הגנת PIN — אי אפשר
   למנוע כיבוי שלו בלי הרשאות Device-Owner, אבל אפשר לוודא שההורה יגלה: באנר אזהרה
   אדום חדש בראש הגדרות ההורה (`renderEnforcementWarning`) שמופיע כשהאכיפה שבורה,
   מפרט מה חסר, וכפתור שפותח ישר את מסך ההגדרות הנכון.

*מה עדיין לא בר-אכיפה בלי Device Owner (בכנות):* הסרת האפליקציה, Force-Stop ידני
מתמשך, וכיבוי מתג הנגישות — שלושתם דורשים ניהול מכשיר מלא (למשל Google Family Link
כשכבה משלימה: לחסום הסרת אפליקציות ולנעול שינוי הגדרות). ההגנה בפועל: הילד לא יודע
לצפות שהחסימה מגיעה משירות נגישות, וההורה מקבל התרעה גלויה ברגע שמשהו כובה.

*אימות:* Kotlin — קומפילציה מלאה של שני ה-flavors; JS — 29/29 בדיקות Playwright +
אימות ידני בדפדפן עם גשר מדומה (הזרעה בהפעלה ובמסך משחקים, באנר האזהרה מופיע/נעלם
נכון, הכפתור פותח את ההגדרות הנכונות). **לא נבדק על מכשיר אמיתי** — נוספו סעיפי AN9.

**עדכון 2026-07-20 — בוצעו A1, A2, A6 (ליבת הלוגיקה האוטיסטית):**
- **A1 ("עכשיו → אחר כך" בכל מסך):** ה-`#firstThenWrap` הועבר ב-`index.html` מתוך `#view-home`
  לשכבת ה-chrome המשותפת (בין `.topbar` ל-`.main`), כך שהוא נראה בכל מסך ילד (לא רק בית).
  `renderFirstThen()` ו-`go()` שניהם משתמשים עכשיו בקבוע משותף `FIRSTTHEN_HIDDEN_VIEWS`
  (`['picker','welcome','admin']`) כדי להסכים על אילו מסכים זה מוסתר. גם המרווח האנכי
  התקופתי (`clockInterval`, לתפיסת מעברי תקופה כמו בוקר→צהריים) עוקב אחרי הילד לכל מסך,
  לא רק בית. אומת ידנית: מוצג ב-home/math/rewards, מוסתר ב-picker.
- **A2 (התרעות מדורגות, בלי ניתוק פתאומי):** `GT_WARN_STEPS` שונה מ-`[300,60,10]`
  ל-`[300,120,30]` (5 דק'/2 דק'/30 שנ', תואם בדיוק את מפרט התוכנית) — משפיע על באנר
  ההתרעה הקיים במשחקי ווב. בצד הנייטיבי, `CALM_MESSAGE_DISPLAY_MS` הוארך מ-1.8 שניות
  (קצר מדי לקרוא) ל-10 שניות מלאות, עם כפתור הסגירה מוסתר באותו זמן (מניעה אמיתית של
  מעבר פתאומי, לא רק קוסמטית). **נדחה במכוון לעבודה עתידית:** "עיגול מתרוקן ויזואלי"
  (טיפול גרפי, שייך יותר לחלק ג' G) והצגת טקסט התרעה נפרד בכל סף במסך ה-overlay הנייטיבי
  (מעבר לצבע הרקע הקיים) — נדרש עבודה נוספת שלא נבדקה על מכשיר; העיצוב הצבעוני
  הרציף הקיים (ירוק→ענבר→אדום) כבר נותן איתות מדורג בטוח.
- **A6 (מצב רגוע):** ביקורת יסודית העלתה שהתשתית **כבר קיימת ומיושמת בעדינות** —
  קונפטי מוחלש (לא מבוטל לגמרי) בפרצי מטבע רגילים, מבוטל לגמרי בחגיגות גדולות
  (תגים/מפגש מושלם); עוצמת צליל מוחלשת; אנימציות רקע (theme-deco) וכל פולסים אחרים
  מבוטלים לגמרי דרך `body.calm-mode`; מתג הורה מהיר קיים ומסתנכרן בין מכשירים. **הפער
  היחיד שנמצא ותוקן:** קצב ההקראה הקולית לא הואט במצב רגוע — עכשיו `utter.rate` (ווב)
  ו-`engine.setSpeechRate()` (נייטיבי, `ttsSpeak` מקבל פרמטר `rate` חדש) יורדים ל-0.75
  כש-`state.calmMode` פעיל, במקום 0.9 קבוע.
- כל 29 בדיקות Playwright עוברות; שני ה-flavors נבנים בהצלחה; אומת ידנית בדפדפן.

**עדכון 2026-07-19 (3) — בוצעו AN1-AN9 (כל חלק א'):**
- **AN1 (TTS נייטיבי):** `NativeGameBridge.kt` מפעיל את `TextToSpeech` האנדרואידי עם
  `UtteranceProgressListener` — `onRangeStart` (API 26+) משדר מיקום מילה חזרה ל-JS דרך
  `window._nativeTtsBoundary`, `onDone`/`onError` דרך `window._nativeTtsEnd`. ב-`app.js`,
  `speakWithHighlight()` מעדיף את הנתיב הזה כש-`nativeTtsAvailable()` (בודק `ttsAvailable()`
  מהגשר) ונופל חזרה ל-`speechSynthesis` הרגיל אחרת — אותה לוגיקת הדגשת מילים משותפת
  (`highlightWordAt`) לשני הנתיבים.
- **AN2 (Splash):** `androidx.core:core-splashscreen`, `installSplashScreen()` לפני
  `super.onCreate`, theme `Theme.CoinQuest.Splash` עם אייקון המטבע הקיים (כבר עגול,
  מתאים למסכת הפיצ'ר בלי צורך בגרפיקה חדשה).
- **AN3 (אופליין):** `sw.js` כבר כיסה את רוב המקרים (network-first + cache fallback);
  הפער היחיד היה עליית קרה ראשונה בלי רשת — נוסף `assets/offline.html` +
  `WebViewClient.onReceivedError` (main-frame בלבד).
- **AN4 (נעילה רכה):** `swipeRefresh.isEnabled=false` (מנע איפוס שאלה בטעות ע"י גרירה),
  `onBackPressed` מציג `AlertDialog` אישור לפני יציאה כשאין היסטוריה. כפתור "🔄 רענון
  האפליקציה" נוסף בהגדרות ההורה כתחליף ידני.
- **AN5 (תזכורות):** `ChoreReminderScheduler`+`ReminderReceiver` חדשים — `AlarmManager`
  יומי לא-מדויק (לא דורש הרשאה מיוחדת ב-Android 12+) עם החזרה אוטומטית אחרי איתחול
  מכשיר (`BOOT_COMPLETED`). כרטיס הגדרות חדש (נסתר בדפדפן רגיל) לבחירת שעה. אזהרת
  "2 דקות" למשחק **כבר הייתה קיימת** משני הצדדים (`GT_WARN_STEPS` בווב, צבעי
  overlay בנייטיב) — לא נדרשה תוספת.
- **AN6 (מסך דלוק):** `keepScreenOn()` בגשר + `updateKeepScreenOn()` ב-`app.js`,
  מופעל בכניסה למסך למידה או משחק (web/native), מכובה ביציאה.
- **AN7 (הקשחה):** תוקנו שני פערים אמיתיים שנמצאו: `allowBackup` היה `true` (סיכון
  שחזור PIN/מטמון מקומי למכשיר אחר) — שונה ל-`false`; `network_security_config.xml`
  היה קיים אך **מעולם לא חובר** ל-manifest (`android:networkSecurityConfig` חסר) —
  חובר עכשיו. חתימת release/keystore כבר היו קיימים ומאובטחים נכון (`.gitignore` מכסה
  `*.jks`/`keystore.properties`, אומת ב-`git ls-files`).
- **AN8 (עדכון-עצמי):** `getVersionCode()` בגשר; `build-apk.yml` כותב `version.json`
  (מ-`versionCode`/`versionName` שב-`build.gradle`) ומפרסם GitHub Release בשם
  `latest-family`; `app.js` בודק פעם ביום ומציג באנר במסך ההורה בלבד. **הערה חשובה:**
  זו התרעה בלבד, לא עדכון אוטומטי — ה-APK שה-CI בונה חתום ב-debug ולא ניתן להתקנה
  כעדכון מעל בילד release חתום אמיתי (התנגשות חתימה); הבאנר מפנה לריפו להורדה ידנית.
- **AN9:** צ'קליסט הבדיקות עודכן עם סעיף ייעודי לכל אחד מ-AN1-AN8.
- **מגבלה חשובה בכל הסעיף הזה:** כל שינויי הקוד הנייטיבי אומתו רק בקומפילציה מקומית
  מוצלחת (`./gradlew assembleDebug` לשני ה-flavors, כולל בדיקת merged manifest) ובבדיקה
  ידנית בדפדפן עם `window.CoinQuestNative` מדומה — **לא נבדקו בפועל על מכשיר/אמולטור
  אנדרואיד אמיתי** (אין גישה לכזה בסביבה הזו). ראו AN9 המעודכן למה חייבים לבדוק ידנית.
- כל 29 בדיקות Playwright עוברות; שני ה-flavors נבנים בהצלחה עם ואחרי כל שינוי.

**עדכון 2026-07-19 (2) — בוצעו S4-S8 (שאר חסמי הפרסום):**
- **S4 (מדיניות Families):** נוספה שורת הבהרה במסך הכניסה ("👪 למסך זה: הורה/אפוטרופוס בלבד"). שאר הדרישות (שער-הורים דרך PIN להגדרות, בלי פרסומות, Google Sign-In רק להורה) כבר היו נכונות בקוד הקיים — לא נדרש שינוי.
- **S5 (מחיקת חשבון):** נוסף `confirmDeleteAccount()`/`deleteAccountAndFamily()` ב-`app.js` — כפתור בהגדרות ההורה (מאחורי PIN) עם אימות הקלדה ("מחק") לפני מחיקה בלתי-הפיכה של `families/$familyId`, `users/{uid}` וקוד ההזמנה המשויך, ואז מחיקת רשומת ה-Auth עצמה (עם טיפול ב-`auth/requires-recent-login`). נוסף גם עמוד ציבורי עצמאי [delete-account.html](delete-account.html) (Firebase client SDK ישיר, בלי backend) לבקשת מחיקה גם בלי להתקין את האפליקציה — כפי שדורש Play. **תוקן תוך כדי:** כלל ה-`inviteCodes` ב-`database.rules.json` לא אפשר מחיקה (רק יצירה) — עודכן לאפשר גם למחוק קוד הזמנה שכבר שייך למשפחה שלך.
- **S6/S7 (הפרדת build לפי ערוץ הפצה):** נוספו שני flavors ל-`android-app/app/build.gradle` — `family` (ברירת המחדל הקיימת, כולל אכיפת זמן-משחק נייטיבית מלאה, לסייידלוד ביתי, אותו applicationId כדי לשמר עדכון-במקום למכשירים הקיימים) ו-`play` (עם `applicationIdSuffix ".play"`, ו-`src/play/AndroidManifest.xml` שמסיר לגמרי את `SYSTEM_ALERT_WINDOW`/`FOREGROUND_SERVICE*`/שני השירותים הנייטיביים דרך `tools:node="remove"`). **נבדק בפועל:** בנייה מקומית של שני ה-flavors הצליחה (`./gradlew assembleDebug`), והמניפסט הממוזג של `play` אומת כריק מהרשאות/שירותים אלה בעוד `family` שומר עליהם. `NativeGameBridge.kt` לא שונה כלל — `startNativeSession` כבר מסרב להתחיל סשן שאי אפשר לאכוף (`hasOverlayPermission()`/`hasAccessibilityPermission()` פשוט מחזירות false כשההרשאה לא קיימת), כך שההתנהגות נגררת אוטומטית. `.github/workflows/build-apk.yml` עודכן להעלות שני ה-APK-ים בנפרד (הנתיב הישן `apk/debug/app-debug.apk` כבר לא קיים).
- **S8a (צ'אט AI):** לשונית הצ'אט בתפריט התחתון עכשיו **מוסתרת כברירת מחדל** (`display:none` קבוע ב-HTML) ומופיעה רק אחרי שהורה מזין מפתח Groq בהגדרות (`updateChatNavVisibility()`). טקסט ההגדרה שוכתב להבהיר שזו "הגדרה מתקדמת... AI חיצוני שאינו בשליטתנו".
- **S8 (צ'קליסט פרסום):** נכתב [PLAY-STORE-CHECKLIST.md](PLAY-STORE-CHECKLIST.md) — כולל פקודת ה-build המדויקת (`./gradlew bundlePlayRelease`, מאומתת שהיא קיימת), טיוטת תשובות לטופס Data Safety, טיוטת טקסטים לדף החנות, ורשימת מה עוד חסר (בעיקר: צילומי מסך אמיתיים ממכשיר). כל שלב שם דורש פעולה ידנית של המשתמש (תשלום, חשבון, מילוי טפסים) — אין לי גישה ל-Play Console.
- כל 29 בדיקות Playwright עוברות; אומת ידנית בדפדפן שכפתור המחיקה נכנס לפעולה רק אחרי הקלדת "מחק" מדויקת, ושלשונית הצ'אט אכן מוסתרת/מוצגת נכון.
- הכללים המתוקנים נפרסו בפועל (אושר ע"י המשתמש). **פעולה נדרשת מהמשתמש:** לעבור על PLAY-STORE-CHECKLIST.md בעצמו החל משלב 1 (חשבון מפתח, צילומי מסך אמיתיים ממכשיר, אישור טקסטים לדף החנות).

**עדכון 2026-07-19 — בוצעו S1, S2, S3 (חסמי הפרסום הראשונים):**
- **S1 (כללי אבטחה בגיט):** נוצר `database.rules.json` (auth חובה + התאמת `users/{uid}.familyId` ל-`families/$familyId`, `inviteCodes` קריא לכל מחובר אך נכתב רק להצביע למשפחה של הכותב) וחובר ל-`firebase.json` (`"database":{"rules":"database.rules.json"}`) כדי שיהיה מנוהל בגיט ונפרס עם `firebase deploy --only database` — לפני כן הכללים היו רק בקונסולה, לא בריפו. **פעולה נדרשת מהמשתמש:** להריץ בפועל `firebase deploy --only database` כדי לפרסם את הקובץ הזה (לא הורצה אוטומטית בסשן זה). גם עודכנה הערה מיושנת ב-`app.js` שאמרה "open test-mode".
- **S2 (חיזוק PIN):** נוספה `isWeakPin()` שחוסמת קודי הורים טריוויאליים (ספרה חוזרת כמו 0000/1111, רצף כמו 1234/4321) בשני מקומות: `savePin()` (הגדרות הורה) ו-`finishWizard()` (הקמת משפחה חדשה). גם שונה placeholder השדה באשף מ-"לדוגמה: 1234" ל"קוד לא מובן מאליו".
- **S3 (הסרת מיתוג מיינקראפט):** שם ה-theme הפנימי שונה מ-`'minecraft'` ל-`'blocks'` בכל הקוד (`childTheme()`, מחלקת CSS `blocks-mode` ב-`styles.css`, תווית הבחירה "⛏️ עולם הבלוקים" במקום "⛏️ מיינקראפט") — עם תאימות לאחור מלאה (משפחות קיימות שנתונם השמור עדיין `'minecraft'` מנורמלות אוטומטית ב-`childTheme()`, בלי צורך במיגרציית נתונים). הוסרה לגמרי הרשומה שהטמיעה iframe ל-`classic.minecraft.net` (הסיכון המשפטי הגדול ביותר) מ-`DEFAULT_GAMES`, כולל מיגרציה חדשה (`cs_games_v5`) שמנקה אותה ממשפחות קיימות + הגנה גם בנתיב הסנכרון מהענן. בשאלות הלמידה (`questions.js`) הוחלפו שמות הדמויות "סטיב"/"אלכס" (הדמויות הרשמיות של מיינקראפט) בשמות גנריים, והוחלף "Creeper" (יצור מוגן) ב-"Zombie" (מושג גנרי מהפולקלור). **הושאר בכוונה:** ערוץ ההשקה הנייטיבי "מיינקראפט (הגרסה שקנית)" (`androidPackage:'com.mojang.minecraftpe'`) — זו שימוש נומינטיבי לגיטימי (הפעלת אפליקציה אמיתית שהמשפחה כבר רכשה בנפרד, לא הטמעה של תוכן של מוג'אנג), ותשם כפי שהוגדר ב-S6 יוצא מגרסת ה-Play ממילא (בלי AccessibilityService).
- כל 29 בדיקות Playwright עוברות ללא שינוי, ואומתו ידנית בדפדפן (theme dropdown, רשימת המשחקים, `isWeakPin`).
- **עדכון:** הכללים נפרסו בפועל (`firebase deploy --only database`, אושר ע"י המשתמש) — `coin-quest-app-default-rtdb` עובד כעת לפי `database.rules.json` שבריפו. S1-S3 הושלמו במלואם.

# תוכנית פיתוח: אפליקציית אנדרואיד מלאה + שיפורי גרפיקה ולוגיקה לילדים

> מסמך עבודה בסגנון DESIGN-IMPROVEMENTS.md: כל סעיף הוא משימה עצמאית, מפורטת לרמה שמודל פשוט
> יכול לבצע אותה אחת-אחת, עם קבצים מדויקים, קוד לדוגמה וקריטריון "בוצע".
>
> **מצב קיים (חשוב! לא בונים מאפס):** בתיקייה `android-app/` כבר קיימת עטיפת WebView עובדת:
> `MainActivity.kt` טוען את `https://coin-quest-app.firebaseapp.com/`, יש גשר נייטיבי
> `NativeGameBridge` (חשוף כ-`window.CoinQuestNative`), אכיפת זמן-משחק נייטיבית
> (`GameTimeOverlayService`, `GameTimeAccessibilityService`), הרשאות מצלמה/התראות,
> ו-workflow של GitHub Actions (`.github/workflows/build-apk.yml`) שבונה `app-debug.apk`.
> לאפליקציית הווב יש `sw.js` (service worker). כל שלב להלן מרחיב את הקיים.

---

## חלק א' — שלבי פיתוח האפליקציה לאנדרואיד (AN1–AN10)

### AN1. הקראה קולית (TTS) אמינה בתוך ה-WebView — קול עברית נייטיבי
**בעיה:** ה-Web Speech API בתוך WebView של אנדרואיד לא תמיד מספק קול עברית (תלוי בחבילת
קולות מותקנת), וההקראה החדשה (`speakWithHighlight` ב-`app.js`) עלולה להיות אילמת במכשיר.
**מה לעשות:**
1. להוסיף ל-`NativeGameBridge.kt` (או קובץ חדש `NativeTtsBridge.kt`) מנוע TTS נייטיבי:
   `android.speech.tts.TextToSpeech` עם `Locale("he","IL")`, ולחשוף לג'אווהסקריפט שתי
   פונקציות: `@JavascriptInterface fun speak(text: String, lang: String, utteranceId: String)`
   ו-`@JavascriptInterface fun stopSpeak()`.
2. להאזין ל-`UtteranceProgressListener` — ב-`onRangeStart(utteranceId, start, end, frame)`
   (זה המקביל הנייטיבי ל-boundary event, נותן אינדקסים של המילה הנוכחית!) לקרוא חזרה
   ל-JS: `webView.evaluateJavascript("window._nativeTtsBoundary&&window._nativeTtsBoundary('${utteranceId}',${start},${end})", null)`
   (על ה-UI thread — לעטוף ב-`webView.post { ... }`). ב-`onDone`/`onError` לקרוא
   `window._nativeTtsEnd('${utteranceId}')`.
3. ב-`app.js`, בתוך `speakWithHighlight()`: אם `window.CoinQuestNative?.speak` קיים —
   להשתמש בו במקום `speechSynthesis` (אותה לוגיקת הדגשת מילים, רק שמקור האירועים הוא
   `_nativeTtsBoundary`/`_nativeTtsEnd` במקום `onboundary`/`onend`). לשמור על מונה
   `_ttsGen` בדיוק כמו היום. `stopSpeaking()` יקרא גם ל-`CoinQuestNative.stopSpeak()`.
4. אם אין קול עברית מותקן (`tts.isLanguageAvailable(...) < 0`) — להחזיר מ-`speak` ערך
   שגיאה, וב-JS ליפול חזרה ל-`speechSynthesis` ואז ל-safety-net timeout הקיים.
**בוצע כאשר:** במכשיר אמיתי שאלה בלמידה מוקראת בעברית עם הדגשת מילים חיה, ותשובה באנגלית
מוקראת בקול אנגלי.

### AN2. אייקון ומסך פתיחה (Splash) מותאמים
**מה לעשות:**
1. להחליף את `ic_launcher_foreground.xml` באייקון של כספת+מטבע בסגנון האפליקציה (אפשר
   וקטור פשוט: כספת סגולה `#7C4DFF` עם מטבע זהב `#FFC107`). לשמור adaptive icon.
2. להוסיף Splash API של אנדרואיד 12+: תלות `androidx.core:core-splashscreen:1.0.1`,
   theme `Theme.SplashScreen` עם `windowSplashScreenBackground` בצבע הרקע של האפליקציה
   ו-`windowSplashScreenAnimatedIcon` = האייקון. ב-`MainActivity.onCreate` לקרוא
   `installSplashScreen()` לפני `setContentView`. זה מסתיר את שנייה-שתיים של טעינת ה-WebView.
**בוצע כאשר:** פתיחת האפליקציה מציגה splash נייטיבי חלק שמתמזג עם מסך הפתיחה הקיים של הווב.

### AN3. מצב אופליין ידידותי לילד
**בעיה:** בלי רשת ה-WebView מציג דף שגיאה טכני באנגלית — מלחיץ לילד אוטיסט.
**מה לעשות:**
1. ב-`MainActivity`, ב-`WebViewClient.onReceivedError` (רק כש-`request.isForMainFrame`):
   לטעון קובץ מקומי `file:///android_asset/offline.html`.
2. ליצור `android-app/app/src/main/assets/offline.html`: עמוד RTL בעברית פשוטה עם אימוג'י
   גדול (📡), טקסט "אין אינטרנט כרגע — נסו שוב עוד רגע", וכפתור ענק "נסה שוב" שקורא
   `location.href='https://coin-quest-app.firebaseapp.com/'`. צבעים ופונט כמו האפליקציה.
3. בנוסף, להרחיב את `sw.js` הקיים כך שיעשה cache ל-`index.html`, `app.js`, `styles.css`,
   `questions.js` (אסטרטגיית stale-while-revalidate) — כך האפליקציה נפתחת גם בלי רשת,
   והסנכרון מתעדכן כשחוזרת.
**בוצע כאשר:** כיבוי WiFi + פתיחת האפליקציה מציג עמוד עברי ידידותי או את האפליקציה מה-cache.

### AN4. נעילת קיוסק רכה (הילד לא יוצא בטעות)
**מה לעשות:**
1. ב-`onBackPressed`: כשאין היסטוריית WebView, במקום לצאת מיד — להציג דיאלוג פשוט בעברית
   "לצאת מהאפליקציה?" עם כפתורים גדולים ✔/✖ (או: לחיצה כפולה על חזרה תוך 2 שניות יוצאת,
   לחיצה בודדת מציגה toast "לחצו שוב כדי לצאת").
2. להוסיף טיפול ב-swipe-refresh: היום כל גרירה למטה מרעננת את העמוד ומאבדת מצב משחק/שאלה.
   לכבות את `swipeRefresh` כברירת מחדל ולהפעיל רענון רק דרך כפתור בהגדרות ההורה
   (`webView.evaluateJavascript` יכול לחשוף `CoinQuestNative.setPullToRefresh(enabled)` —
   פשוט יותר: `swipeRefresh.isEnabled = false` ולהוסיף לגשר פונקציה `reload()`).
**בוצע כאשר:** גרירה למטה באמצע שאלה לא מאפסת אותה, ויציאה מהאפליקציה דורשת פעולה מכוונת.

### AN5. התראות מקומיות: תזכורת מטלות ו"נגמר הזמן בקרוב"
**מה לעשות:**
1. בגשר הנייטיבי להוסיף `@JavascriptInterface fun scheduleReminder(hour: Int, minute: Int, text: String)`
   באמצעות `AlarmManager.setInexactRepeating` + `BroadcastReceiver` שמציג נוטיפיקציה
   (ערוץ נוטיפיקציות "תזכורות", חשיבות DEFAULT — בלי צליל מבהיל, ראו חלק ג').
2. באפליקציית הווב, בהגדרות ההורה: שדה שעה "תזכורת מטלות יומית" שקורא לפונקציה הזו.
3. באכיפת זמן המשחק הקיימת (`GameTimeOverlayService`): 2 דקות לפני סוף הזמן להציג
   נוטיפיקציה + טקסט על ה-overlay "עוד 2 דקות ⏳" — התרעה מוקדמת חשובה במיוחד לילדים
   אוטיסטים (מעבר פתאומי = התמוטטות; ראו A2 בחלק ג').
**בוצע כאשר:** נוטיפיקציית תזכורת מופיעה בשעה שנקבעה, והתרעת "2 דקות" מופיעה לפני סוף משחק.

### AN6. שמירת מסך דולק בזמן שאלה/משחק
**מה לעשות:** בגשר: `keepScreenOn(on: Boolean)` שמפעיל/מכבה
`window.addFlags(WindowManager.LayoutParams.FLAG_KEEP_SCREEN_ON)` (דרך `runOnUiThread`).
ב-`app.js`: להדליק בכניסה ל-view של למידה/משחק (`go('learn')`), לכבות ביציאה.
**בוצע כאשר:** המסך לא נכבה באמצע שאלה גם אחרי timeout של המערכת.

### AN7. גרסת release חתומה + הקשחה
**מה לעשות:**
1. ליצור keystore (`keytool -genkeypair ... -validity 10000`), לשמור **מחוץ לריפו**,
   ולהוסיף `signingConfigs.release` ב-`app/build.gradle` שקורא סיסמאות מ-`local.properties`
   או ממשתני סביבה (ב-CI: GitHub Secrets + שלב `assembleRelease`).
2. להשאיר `minifyEnabled false` בשלב ראשון (לקח מפרויקט tap_runner: R8 שבר build שם;
   אם מפעילים — חובה בדיקת sideload לפני העלאה).
3. `android:allowBackup="false"` במניפסט (שלא ישחזרו נתוני PIN ממכשיר אחר),
   `versionCode`/`versionName` מנוהלים ידנית בכל שחרור.
**בוצע כאשר:** `./gradlew assembleRelease` מפיק APK חתום שמותקן ורץ במכשיר אמיתי.

### AN8. עדכון-עצמי פשוט (בלי חנות)
כי ההפצה היא sideload משפחתי, לא Google Play:
1. בכל build של CI לכתוב קובץ `version.json` (`{"versionCode": N, "apkUrl": "..."}`)
   ל-GitHub Releases (או לתיקיית ההפצה של Firebase Hosting).
2. בגשר: `getVersionCode()`. ב-`app.js`, פעם ביום: fetch ל-`version.json`, ואם גרסת
   הענן גדולה — להציג להורה (רק במסך ההורה!) באנר "יש עדכון לאפליקציה" עם קישור להורדה.
**בוצע כאשר:** העלאת גרסה חדשה מציגה באנר עדכון במכשיר תוך יום.

### AN9. בדיקות במכשיר אמיתי — צ'קליסט חובה לכל שחרור
**חשוב: כל שינויי AN1-AN8 עברו רק קומפילציה מקומית מוצלחת (`./gradlew assembleDebug`
לשני ה-flavors) ואימות ידני בדפדפן (עם `window.CoinQuestNative` מדומה) — אין לי גישה
למכשיר Android אמיתי או אמולטור בסביבה הזו, אז אף אחד מהפיצ'רים הנייטיביים החדשים
לא נבדק בפועל עם קול/מסך/מגע אמיתיים. הצ'קליסט הזה הוא בדיקת החובה שמכסה את הפער.**

לרוץ ידנית לפני כל APK שנמסר לילדים (build ה-`family`, לא `play`):
- [ ] כניסת PIN הורה עובדת; כניסת ילד עם לחיצה אחת.
- [ ] סימון מטלה → אנימציית מטבע → יתרה עולה; סימון שני תוך דקה נחסם (cooldown).
- [ ] קניית זמן משחק → הזמן מופיע בבאנר → נכנס למשחק → הטיימר יורד → הזמן נאכף (overlay).
- [ ] שאלה בלמידה מוקראת בקול עם הדגשת מילים; כפתור 🔊 חוזר על ההקראה; תשובה עוצרת הקראה.
- [ ] סריקת QR במצלמה עובדת (הרשאה + פוקוס).
- [ ] מכשיר שני: שינוי בהגדרות הורה מסתנכרן תוך שניות.
- [ ] מצב טיסה: עמוד אופליין ידידותי; חזרת רשת: סנכרון מתאושש.
- [ ] סיבוב מסך/מעבר לאפליקציה אחרת וחזרה: המצב לא אבד.

**חדש — לבדוק במיוחד (AN1-AN8, טרם נבדקו על מכשיר אמיתי):**
- [ ] **AN1:** שאלה בלמידה מושמעת בקול עברי אמיתי (לא שקטה) גם אם למכשיר אין חבילת קול
  מותקנת ל-Web Speech; ההדגשה של המילה הנוכחית מסתנכרנת עם הקול (לא רק על מכשירים
  ישנים מ-Android 8, ששם אין הדגשה חיה — רק הקראה שלמה בלי הדגשה, וזה תקין).
- [ ] **AN2:** פתיחת האפליקציה מציגה מסך פתיחה (splash) עם המטבע לפני טעינת ה-WebView,
  לא מסך לבן.
- [ ] **AN3:** מצב טיסה **בפתיחה הראשונה אי-פעם** (לפני שהאפליקציה נטענה ולו פעם אחת
  עם רשת) מציג את עמוד ה"אין אינטרנט" העברי, לא שגיאת Chromium באנגלית.
- [ ] **AN4:** גרירה למטה בכל מסך לא עושה כלום (בלי ריענון בטעות); לחיצה על "חזרה" בלי
  היסטוריה מציגה דיאלוג אישור יציאה; כפתור "🔄 רענון האפליקציה" בהגדרות עובד.
- [ ] **AN5:** הגדרת שעת תזכורת בהגדרות ← התראה מופיעה בשעה הזו למחרת; **לבדוק גם אחרי
  איתחול מכשיר** שהתזכורת עדיין פעילה (בלי לפתוח את האפליקציה מחדש).
- [ ] **AN6:** המסך לא נכבה מעצמו באמצע שאלה או משחק, גם אחרי המתנה ארוכה.
- [ ] **AN7:** APK חתום (`assembleFamilyRelease`) מתקין ורץ תקין; `allowBackup=false`
  לא שובר שום דבר (בדיקת גיבוי/שחזור ADB לא רלוונטית לילד, רק לוודא שהאפליקציה עצמה
  לא קורסת עם השינוי).
- [ ] **AN8:** אחרי שמפרסמים release חדש ב-GitHub (ה-workflow רץ אוטומטית), באנר
  "גרסה חדשה זמינה" מופיע במסך ההורה תוך יממה במכשיר עם גרסה ישנה יותר.
- [ ] **אכיפת מיינקראפט (עדכון 2026-07-20 (2)) — קריטי לבדוק:**
  - התקנה טרייה של האפליקציה + פתיחת כספת המטבעות פעם אחת (בלי לקנות זמן!) → ניסיון
    לפתוח מיינקראפט ישירות מהמסך הראשי → נחסם מיד עם Toast והחזרה הביתה.
  - קניית 2 דקות → משחק → בזמן הסשן: לכבות את המכשיר באמצע (מדמה קריסה) → הפעלה
    מחדש → ניסיון לפתוח מיינקראפט אחרי שעברו ה-2 דקות → נחסם (בדיקת הדדליין).
  - כיבוי שירות הנגישות בהגדרות המכשיר → פתיחת הגדרות ההורה באפליקציה → באנר האזהרה
    האדום מופיע, והכפתור מוביל למסך ההגדרות הנכון.

### AN10. (אופציונלי, שלב מאוחר) Google Play
רק אם רוצים הפצה מסודרת: חשבון מפתח (25$), `privacy.html`+`terms.html` כבר קיימים בריפו,
טופס Data Safety (האפליקציה שולחת נתונים ל-Firebase RTDB), דירוג תוכן "לכל המשפחה",
מסלול Internal Testing קודם. **הערה:** אכיפת זמן משחק דרך AccessibilityService עלולה
להידחות במדיניות Play — ייתכן שיהיה צורך להצהיר עליה כ"כלי הורים" או להשאיר את הפיצ'ר
רק בגרסת ה-sideload.

---

## חלק ב' — שיפורי נראות: גרפיקה אטרקטיבית לילדים (G1–G8)

> ממשיכים את V1–V10 מ-DESIGN-IMPROVEMENTS.md (בוצעו). כל השיפורים חייבים לכבד את
> `prefers-reduced-motion` ואת דגלי ההרגעה של חלק ג'.

### G1. דמות מלווה (מסקוט) לכל ילד
דמות SVG קטנה וקבועה שמופיעה בפינת המסך: לאריאל — קריפר/גולם ידידותי בסגנון בלוקים
(בלי קווים חדים/מפחידים); לנועה — דמות מהעולם שלה. הדמות מגיבה באירועים: תשובה נכונה →
קופצת (CSS `transform: translateY` keyframes, 400ms); סימון מטלה → מוחאת כפיים (החלפת
frame); חוסר פעילות 30 שניות במסך למידה → מנפנפת. מימוש: `<div id="mascot">` קבוע
ב-`index.html`, פונקציה `mascotReact(kind)` ב-`app.js`, שני קבצי SVG inline לפי theme.
**חשוב:** הבעות פנים פשוטות וחד-משמעיות בלבד (שמח/רגוע) — לא הבעות מעורפלות.

### G2. פס התקדמות יומי חזותי ("מפת מסע")
במקום מספרים בלבד: שביל אופקי עם 5–8 תחנות (עיגולים), כל מטלה שהושלמה צובעת תחנה ומזיזה
את דמות הילד תחנה קדימה. בסוף השביל — תיבת אוצר שנפתחת (אנימציית CSS) כשכל המטלות הושלמו.
מימוש בתוך `renderDayStrip()` הקיים; תחנות = `flex` של spans, הדמות = אימוג'י/SVG קטן
עם `transition: right .6s ease`.

### G3. חנות פרסים ויזואלית עם "מד חיסכון"
לכל פרס בחנות: עיגול התקדמות (SVG `stroke-dasharray`) שמראה כמה מהמחיר כבר יש לילד —
"יש לך 70 מתוך 100 🪙". פרס בר-השגה מקבל זוהר זהוב עדין (`box-shadow` פועם — סטטי אם
reduced-motion). זה הופך חיסכון ממושג מופשט לתמונה — קריטי לגיל 7 ולחשיבה קונקרטית.

### G4. אווטאר מטבע מתפתח
היתרה מוצגת עם ערימת מטבעות מצוירת שגדלה במדרגות (1–3 מטבעות / שקית / תיבה / כספת
זוהרת לפי גובה היתרה). מימוש: פונקציה `coinPileFor(balance)` שמחזירה אחד מ-4 SVG,
מוחלפת ב-`renderBalance()` עם `crossfade` קצר.

### G5. חגיגות מדורגות (לא מבהילות)
שלוש רמות חגיגה: תשובה נכונה = ✔ ירוק + צליל רך קצר; השלמת סשן למידה = גשם קונפטי עדין
2 שניות; שיא אישי חדש = מסך "כל הכבוד!" עם הדמות המלווה. **אף פעם לא:** הבזקים,
רעידות מסך, צלילים חזקים פתאומיים. עוצמת החגיגה נשלטת בדגל `state.settings.celebrationLevel`
(ראו A6).

### G6. ערכת אייקונים אחידה למטלות
לכל מטלה אימוג'י/אייקון גדול (48px+) שההורה בוחר מרשימה סגורה בעת יצירת המטלה
(🦷🛏️🎒📚🍽️🧸🚿...). ילד שלא קורא מזהה מטלה לפי תמונה. שדה `icon` באובייקט המטלה,
בורר אייקונים בטופס ההורה, תצוגה ב-`renderChores()`.

### G7. רקעים חיים לפי שעת יום
הרקע של מסך הבית משתנה עדין לפי שעה: בוקר (שמיים בהירים), צהריים, ערב (שקיעה), לילה
(כוכבים) — עוזר גם לעיגון בזמן (ילדים אוטיסטים נשענים על עוגני שגרה). מימוש: 4 מחלקות
CSS gradient על `body`, נבחרות ב-`applyTheme()` לפי `new Date().getHours()`.

### G8. משוב מגע (haptic) עדין
`navigator.vibrate(20)` (או דרך הגשר הנייטיבי) בסימון מטלה ובתשובה נכונה — רטט קצרצר
אחד, לא תבניות. כבוי כברירת מחדל אם `state.settings.calmMode` פעיל, וניתן לכיבוי בהגדרות.

---

## חלק ג' — שיפורי לוגיקה מותאמים לילדים אוטיסטים (A1–A8)

> עקרונות מנחים: צפוּיוּת (predictability), מעברים מוכנים מראש, עומס חושי מינימלי,
> אפס ענישה, שפה קונקרטית וחיובית.

### A1. "עכשיו → אחר כך" בכל מסך
בכל רגע הילד רואה בדיוק שני דברים: מה עושים עכשיו ומה יקרה אחר כך ("עכשיו: 3 שאלות
חשבון → אחר כך: 10 דקות משחק"). להרחיב את `renderFirstThen()` הקיים כך שיופיע כרצועה
קבועה בראש **כל** מסך ילד, לא רק במסך הבית, עם שני כרטיסים מחוברים בחץ.

### A2. טיימרים חזותיים + התרעות מעבר מדורגות
כל ספירה לאחור (זמן משחק, זמן לשאלה) מוצגת כעיגול מתרוקן ויזואלי, ולפני כל מעבר יש
שלוש התרעות עדינות: 5 דקות ("עוד מעט מסיימים"), 2 דקות, 30 שניות — טקסט + הדמות המלווה
מצביעה על השעון. אף פעם לא ניתוק פתאומי: בסוף הזמן מסך "הזמן נגמר, היה כיף! 👋" ל-10
שניות לפני החזרה למסך הבית (ה-overlay הנייטיבי הקיים יקבל את אותה התנהגות).

### A3. שגרה קבועה ולוח יומי צפוי
מסך "היום שלי": רשימת המטלות תמיד באותו הסדר (ההורה קובע סדר, לא מיון אוטומטי!),
מטלה שהושלמה לא נעלמת אלא מקבלת ✔ גדול ונשארת במקומה — היעלמות פריטים שוברת צפיות.
שדה `order` למטלות + drag-to-reorder בטופס ההורה בלבד.

### A4. אפס ענישה, רק חיזוק
לוודא שאין בשום מקום ירידת מטבעות/נקודות אוטומטית: תשובה שגויה בלמידה = "בוא ננסה
שוב 💪" + הצגת התשובה הנכונה עם הקראה, בלי X אדום גדול ובלי צליל שלילי. אחרי 2 טעויות
באותה שאלה — השאלה "מתחלפת" (לא נכשלת) ומופיעה שוב מאוחר יותר קלה יותר (ראו A5).

### A5. הסתגלות קושי הדרגתית ושקטה
מנגנון פשוט ב-`answerLearningQuestion`: רצף 3 נכונות → עולים תת-רמה; 2 שגיאות → יורדים
תת-רמה, **בלי להודיע לילד** (שינוי רמה מוצהר = תסכול). לשמור `state.learning.adaptiveLevel`
פר-נושא פר-ילד. ההורה רואה את הרמה בפועל במסך ההורה בלבד.

### A6. "מצב רגוע" (Calm Mode) — מתג הורה אחד
דגל `state.settings.calmMode` שמכבה בבת אחת: קונפטי, רטט, צלילי חגיגה, אנימציות רקע
חיות, ומאט את קצב ההקראה (`utter.rate=0.8`). שקול ל-`prefers-reduced-motion` אבל ברמת
האפליקציה ונשלט על ידי ההורה — יש ימים רגישים. כפתור מהיר במסך ההורה + סנכרון בין מכשירים.

### A7. הכנה למעברים בין מסכים
מעבר בין views לא קורה מיידית בלחיצה על משהו שמסיים פעילות: קודם מסך ביניים קצר (1.5
שניות) "מסיימים... עוברים ל..." עם אייקון היעד. מימוש: wrapper ל-`go(v)` —
`goWithTransition(v, label)` שמציג overlay קצר. חל רק על מעברים "גדולים" (סיום למידה,
סיום משחק), לא על ניווט רגיל.

### A8. תקשורת חזותית עקבית — מילון סמלים אחיד
לקבע: 🪙 תמיד מטבעות, ⏳ תמיד זמן, ✔ ירוק תמיד הצלחה, 🎁 תמיד פרס — ולעבור על כל
המסכים לוודא שאין סמל שמשמש לשני דברים או שני סמלים לאותו דבר. בנוסף: כל כפתור פעולה
של ילד = אייקון + מילה אחת בלבד (לא משפטים), גודל מגע מינימלי 48px, מרווח בין כפתורים
שמונע לחיצות שגויות.

---

## חלק ד' — התאמות חובה לפרסום ב-Google Play (S1–S8)

> **עקרון העלויות:** אין צורך לשלם לאף גורם שלישי. חשבון Firebase נשאר בתוכנית **Spark
> (חינם)** — Auth, Realtime DB (עד 1GB / 100 חיבורים בו-זמנית) ו-Hosting כולם בחינם.
> הרשמת משתמשים כבר ממומשת בתוך האפליקציה (Google Sign-In + familyId + קודי הזמנה,
> `app.js` ~3583) — אין תלות בשירות רישום חיצוני. העלות היחידה: 25$ חד-פעמי לחשבון
> מפתח ב-Play. חלופה של "הכל מקומי במכשיר בלבד" הייתה מוותרת על סנכרון בין מכשירי
> ההורים והילד — לכן נשארים עם Firebase החינמי.
>
> **חסם קנה-מידה לידיעה בלבד:** מגבלת 100 חיבורים בו-זמנית ב-Spark מספיקה לעשרות-מאות
> משפחות פעילות. אם האפליקציה תצליח מעבר לזה — שדרוג ל-Blaze (תשלום לפי שימוש) הוא
> החלטה עתידית, לא שינוי ארכיטקטורה.

### S1. ביקורת Security Rules — לוודא ולתעד, לפני כל שיווק
כללים סגורים כבר נפרסו ב-2026-07-01 (auth חובה + התאמת `users/{uid}.familyId`), אבל:
(א) הם קיימים רק בקונסולת Firebase, לא בריפו — להוריד אותם לקובץ `database.rules.json`
ולחבר ל-`firebase.json` כדי שיהיו מנוהלים בגיט ונפרסים עם `firebase deploy`;
(ב) יש הערה מיושנת ב-`app.js` ~3447 שעדיין אומרת "open test-mode" — לעדכן;
(ג) לבצע ביקורת אימות מחדש לפני שיווק. הכללים הנדרשים:
- `families/$familyId/**`: קריאה/כתיבה רק אם `root.child('users/'+auth.uid+'/familyId').val() === $familyId`.
- `users/$uid`: קריאה/כתיבה רק אם `auth.uid === $uid` (יצירה ראשונית: כתיבה מותרת רק ל-uid עצמו).
- `inviteCodes/$code`: קריאה לכל משתמש מחובר (`auth != null`), כתיבה רק ע"י חבר במשפחה שהקוד מצביע עליה.
- ברירת מחדל: הכל חסום.
לפרוס דרך `firebase deploy --only database`, ולבדוק: מכשיר של משפחה א' לא מצליח לקרוא
נתוני משפחה ב' (בדיקה ידנית עם שני חשבונות Google).
**בוצע כאשר:** בקשת קריאה ל-familyId זר מחזירה permission_denied, וכל הבדיקות הקיימות עוברות.

### S2. חיזוק PIN הורים
משפחות חדשות כבר בוחרות PIN משלהן ב-onboarding (נוסף 2026-07-01) — הבעיה נותרה רק
בנתוני המשפחה המקורית. לגרסה ציבורית: (א) לוודא שה-PIN נשמר כ-hash (SHA-256 עם salt
אקראי פר-משפחה, `crypto.subtle.digest`) ולא בטקסט גלוי בסנכרון; (ב) לחסום PIN-ים
טריוויאליים ביצירה (1234, 0000, ארבע ספרות זהות); (ג) שכחת PIN = שחזור דרך אימות מחדש
בחשבון Google של הבעלים. (ה-PIN הוא שער-הורים בפני הילד, לא אבטחה קריפטוגרפית — ה-hash
מספיק.)

### S3. הסרת מיתוג Minecraft — סיכון קניין רוחני
יש ~31 אזכורי "Minecraft/מיינקראפט" ב-`index.html`, `app.js`, `questions.js`, ותיקיית
`games/`, כולל iframe ל-`classic.minecraft.net`. שימוש בשם ובמותג של Mojang באפליקציה
מסחרית בחנות = דחייה או הסרה בתלונת IP. מה לעשות:
1. לשנות את שם הערכה ל"עולם הבלוקים" (הסגנון הוויזואלי הבלוקי עצמו חוקי — סגנון אמנותי
   אינו מוגן; השם, הלוגו והדמויות המקוריות כן).
2. להסיר מהגרסה החנותית את ה-iframe של `classic.minecraft.net` ולהשאיר משחקים חופשיים
   (ClassiCube שכבר קיים ב-`games/` הוא קוד פתוח — לוודא עמידה ברישיון שלו וייחוס).
3. בשאלות (`questions.js`): לנסח מחדש שאלות שמזכירות Minecraft בשמו לדמויות גנריות
   ("בעולם הבלוקים...").
**בוצע כאשר:** `grep -ri minecraft` על קבצי הפריסה מחזיר 0 תוצאות.

### S4. עמידה במדיניות Families של Google Play
אפליקציה שקהל היעד שלה כולל ילדים חייבת:
1. **הצהרת קהל יעד:** "ילדים ומבוגרים" (ההורה משתמש גם) → נכנסים אוטומטית לדרישות Families.
2. **שער הורים (Parental Gate):** כל קישור חיצוני, מסך הגדרות, והתחברות Google חייבים
   להיות מאחורי אימות מבוגר (ה-PIN מ-S2 עונה על זה — לוודא שגם מסך ה-sign-in הראשוני
   מנוסח כפנייה להורה).
3. **איסוף נתונים מילדים:** Google Sign-In הוא של **ההורה** בלבד; פרופילי הילדים הם שם
   פרטי + נתוני משחק, בלי אימייל/טלפון של הילד. לתעד זאת ב-Data Safety וב-privacy.html
   (קיים — לעדכן תוכן: אילו נתונים, איפה נשמרים, זכות מחיקה).
4. **בלי פרסומות** בשלב ראשון — מפשט דרמטית את האישור. אם בעתיד רוצים מונטיזציה:
   רכישה חד-פעמית/מנוי להורים (מאחורי שער הורים) בטוחה יותר ממודעות.
5. **WebView שטוען תוכן מרוחק:** מותר, אבל התוכן הנטען חייב לעמוד באותה מדיניות —
   עוד סיבה ל-S3 (אין להטעין אתרי צד-שלישי בלתי-מבוקרים בתוך אפליקציית ילדים).

### S5. מחיקת חשבון — דרישת חובה של Google
כל אפליקציה עם יצירת חשבון חייבת לספק: (א) מחיקת חשבון מתוך האפליקציה, (ב) קישור URL
למחיקה גם מחוץ לאפליקציה (מוזן בטופס ב-Play Console). מימוש: כפתור "מחק את החשבון
והמשפחה" במסך ההורה (אחרי PIN + אישור כפול) שמוחק `families/$familyId`, `users/$uid`,
וקורא `user.delete()`; ועמוד `delete-account.html` ב-Hosting עם הוראות (או טופס מחיקה
מקוון עם sign-in).

### S6. אכיפת זמן משחק (AccessibilityService) — לפצל גרסאות
שימוש ב-AccessibilityService למטרה שאינה נגישות נדחה כמעט תמיד ב-Play, וגם
`SYSTEM_ALERT_WINDOW` מעורר בדיקה. פתרון: **שני build flavors** ב-`app/build.gradle`:
- `play`: בלי `GameTimeAccessibilityService`, בלי `SYSTEM_ALERT_WINDOW` — אכיפת הזמן
  נשארת ברמת האפליקציה בלבד (הטיימר הפנימי הקיים ב-web).
- `family`: הגרסה המלאה עם האכיפה הנייטיבית, מופצת ב-sideload כמו היום.
כך אין ויתור על הפיצ'ר בבית ואין סיכון דחייה בחנות.

### S7. הצהרות הרשאות
בגרסת `play` להשאיר רק: INTERNET, CAMERA (עם הצהרת שימוש בטופס: "סריקת קודי QR לאישור
מטלות ע"י ההורה"), POST_NOTIFICATIONS. להסיר מהמניפסט של flavor זה את הרשאות השירותים
המיוחדים. מצלמה חייבת `android:required="false"` (כבר קיים).

### S8a. צ'אט ה-AI (מפתח Groq) בגרסה הציבורית
היום הורה מדביק מפתח Groq משלו בהגדרות. פתרון-ביניים ללא עלות וללא backend: להסתיר את
הפיצ'ר כברירת מחדל בגרסת החנות ולהציג אותו כ"הגדרה מתקדמת להורים" עם הסבר איך משיגים
מפתח חינמי — או להשמיט אותו מגרסת `play` לגמרי. proxy משותף (Cloud Function) דורש
תוכנית Blaze — נדחה עד שיש הכנסות.

### S8. תהליך הפרסום עצמו
1. חשבון מפתח (25$) → 2. `assembleRelease` חתום (AN7) בפורמט **AAB** (`bundleRelease`),
זכור: minify כבוי או בדיקת sideload לפני העלאה (לקח tap_runner) → 3. דף חנות בעברית
(+אנגלית): צילומי מסך, גרפיקה, תיאור ללא הבטחות רפואיות/טיפוליות — לנסח "מותאם לילדים
עם צרכים מיוחדים", לא "טיפול באוטיזם" → 4. שאלון דירוג תוכן → 5. Data Safety → 6.
מסלול Internal testing (עד 100 בודקים, בלי ביקורת מלאה) → 7. Closed testing — **דרישה
חדשה לחשבונות אישיים: 12 בודקים למשך 14 יום לפני production** → 8. Production.

---

## סדר ביצוע מומלץ
0. **S1, S2** (סגירת האבטחה — לפני כל שיווק; חוסם מוחלט) ואז **S3** (הסרת מיתוג Minecraft)
1. **AN1** (TTS נייטיבי — משלים את פיצ'ר ההקראה שרק עכשיו נוסף) → **AN3, AN4** (יציבות בסיסית לילד)
2. **A1, A2, A6** (הליבה האוטיסטית: צפיות, מעברים, מצב רגוע)
3. **G1, G2, G6** (מסקוט, מפת מסע, אייקוני מטלות — האפקט הוויזואלי הגדול ביותר)
4. **AN2, AN5, AN6, A3, A4, A5** → **G3–G5, G7, G8, A7, A8** → **AN7, AN8, AN9** (שחרור)

כל משימה: מימוש → בדיקה ידנית בדפדפן/מכשיר → בדיקת Playwright אם רלוונטי → commit נפרד.
