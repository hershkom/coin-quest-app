# תוכנית שיפור נראות ואווירה — כספת המטבעות
*נכתב 2026-07-14. מבוסס על מחקר עיצוב אפליקציות לילדים (יחס צבע 60-30-10, טעויות שלא מרגישות כישלון, משוב חי אך לא מציף), הנחיות עיצוב חושי לרצף האוטיסטי (להימנע מהבהובים ותנועה מתמדת, לאפשר כיבוי אנימציות, אדומים/צהובים עזים מציפים, ירוקים/כחולים מרגיעים), וטכניקות UI בסגנון מיינקראפט (פינות מרובעות, מסגרות bevel תלת-ממדיות, `image-rendering:pixelated`).*

**עדכון 2026-07-14 — בוצע שלב 1 (V1, V5, V8):** `@media (prefers-reduced-motion:reduce)` גלובלי + `coinBurst()` מדלג לגמרי על הפיצוץ כשמופעל (לא רק מכבה CSS — נבדק ידנית עם `matchMedia` מדומה); הורחבה רשימת `.app.calm-mode` (כולל `.learn-dot.active`); `coinBurst()` הורד מ-14 ל-12 חלקיקים + safety-net `setTimeout` cleanup נוסף מעל ה-`onfinish` הקיים (נבדק ידנית: 0 שאריות DOM אחרי 2.5 שניות); `--muted` הוכהה מ-`#9B96B0` ל-`#807A99` לניגודיות; `.icon-btn`/`.chat-speak-btn` הוגדלו מ-34px/30px ל-44px. נבדק ידנית בפריוויו + 23/23 בדיקות Playwright עוברות. **עדכון 2026-07-14 (2) — בוצע שלב 2 (V4, V9 חלקי):** `coinFly(fromRect)` חדש — מטבע בודד עף פיזית (Web Animations API) מנקודת הלחיצה אל `#balTop`, במקום `coinBurst()` הכללי, בכל שלוש נקודות ההרווחה: `markChore` (מ-`.chore-check` שנלחץ), `mathCheck` (מ-כפתור ה-"✓"), `answerLearningQuestion` (מכפתור התשובה/הגשה). תוקן באג טיימינג אמיתי תוך כדי המימוש: `renderChores()` הורס ובונה מחדש את ה-DOM (`innerHTML=''`) *מיד* אחרי הקריאה ל-`addPoints`, אז snapshot של ה-rect חייב לקרות באופן סינכרוני *בתוך* `addPoints` (לפני כל `await`), אחרת האלמנט כבר מנותק כש-`coinFly` בפועל רץ. נבדק ידנית: מטבע מעופף מופיע ונעלם נקי משלושת המקורות; מכבד `prefers-reduced-motion` (מדולג לגמרי, לא רק מוקטן). `chime()` הורחב לתמוך גם ב-mode `'celebrate'` (ארפג'יו 4 תווים) — משמש בפתיחת תג חדש ובסיום מפגש למידה מושלם, כדי שהרגעים האלה יישמעו שונה ממטבע רגיל. **לא בוצע:** פרופיל `'tap'` לניווט (היה אופציונלי בתוכנית, דורש הגדרת הורה חדשה — דילגתי כדי לא להוסיף UI שאין לו שימוש מוכח). נבדק ידנית + 23/23 בדיקות עוברות.

**עדכון 2026-07-14 (3) — בוצע שלב 3 (V2, V3, V7):**
- **V2:** פינות 4px (לא 0 — "בלוקי" בלי לפגוע בקריאוּת) + מסגרת bevel (בהיר למעלה-שמאל/כהה למטה-ימין, מתחלף ב-`:active`) על `.btn`/`.bigbtn` במצב מיינקראפט; פס דשא/אדמה בשכבות `inset box-shadow` בראש ה-hero. נבדק ידנית: `getComputedStyle` אישר `borderRadius:4px` ו-`borderTopColor`≠`borderBottomColor`.
- **V3:** `addMinecraftDecorations()` נכתב מחדש לגמרי — עננים פיקסליים נעים לאט (90-120 שניות למעבר) + פס דשא/אדמה תחתון, ומצב לילה (רקע כהה + ירח + כוכבים קבועים, בלי תנועה) שמופעל אוטומטית לפי `currentPeriodKey()==='sleep'` הקיים (לא נכתבה לוגיקת שעה חדשה). **תוקן באג ארכיטקטורה אמיתי תוך כדי המימוש:** הדקורציה המקורית הייתה מצורפת ל-`document.body`, אבל ל-`.app` יש רקע אטום `!important` במצב מיינקראפט וברוחב טלפון `.app` ממלא את כל המסך — כך שהדקורציה מעולם לא הייתה יכולה להיראות בפועל במכשיר האמיתי (רק בדסקטופ רחב, דרך השוליים לצדי ה-520px). תוקן ע"י הצמדת `#mc-deco` כילד ראשון בתוך `.app` עצמו (`position:absolute` יחסית אליו) — נבדק ידנית: `deco.parentElement.className` מאשר שהוא בתוך `.app.minecraft-mode`. מצב רגוע קופא את האנימציה (`body.calm-mode #mc-deco *{animation:none}` — נדרש `applyCalmModeClass()` להוסיף גם ל-`body`, לא רק ל-`.app`, כי השכבה יושבת מחוץ להיקף שה-class המקורי כיסה בעבר, ועכשיו כן).
- **V7:** מסך "מכרה הידע" קיבל רקע מערה (גרדיאנט חום כהה) + "עורקי יהלום" דקורטיביים, וסולם כלים חדש (`currentTool()`/`renderLearnToolBar()`) שמוצג גם בראש המסך וגם במסך הסיכום — עץ→אבן→ברזל→זהב→יהלום לפי סה"כ תשובות נכונות מצטבר, קוסמטי בלבד (אין השפעה על מטבעות). נבדק ידנית: מפגש מלא הראה "🪵 עוד 23 תשובות נכונות למכוש אבן" ואז "🪵 הכלי שלך: מכוש עץ" במסך הסיכום.
כל שינוי נבדק גם עם מעבר בין אריאל↔נועה (הדקורציה מתנקה נקי) ו-23/23 בדיקות Playwright עוברות.

**עדכון 2026-07-14 (4) — בוצע שלב 4-5 (V6, V10):**
- **V6:** מנגנון theme הוכלל לכל ילד — `childTheme(ch)` (עם fallback לפי id לילדים ישנים בלי השדה, אותו דפוס בדיוק כמו `childUsesSchedule` הקיים), `applyChildTheme(id)` מרכזי במקום 5 עותקים כפולים של `if(id==='ariel')` שהיו פזורים בקוד (selectChild, מחיקת ילד, שחזור אחרי sign-in, שחזור קר). נועה מקבלת עולם חד-קרן חדש (`.app.unicorn-mode`): רקע פסטלי ורוד-סגול, עננים רכים + כוכבים מנצנצים ב-`#theme-deco` (אותו אלמנט גנרי ששימש קודם רק למיינקראפט — עכשיו `addThemeDecorations(theme)` בונה תוכן שונה לפי הערך). בכוונה **בלי** שינוי גופן/bevel כמו במיינקראפט — עולם קליל יותר. הורה יכול לדרוס את הבחירה לכל ילד דרך תפריט חדש בעריכת ילד (⚙️ הורים → ילדים → ✏️). נבדק ידנית: נועה=חד-קרן, אריאל=מיינקראפט, ילד שלישי היפותטי=ללא עולם, ודריסה ידנית של ההורה חלה מיד.
- **V10:** מסך `#splash` נרנדר ישירות מה-HTML (לפני כל JS), מוסתר ע"י `hideSplash()` שנקראת מתוך `go()` — כלומר בדיוק ברגע שהאפליקציה מגיעה לראשונה למסך אמיתי כלשהו (welcome/picker/בית), לא משנה איזה ענף בבוטסטראפ המורכב (auth/local-only/redirect) הוביל לשם. תקרת בטיחות `setTimeout` של 4 שניות ב-HTML עצמו למקרה שהבוטסטראפ נכשל לפני `go()` ראשון. גם רוככה אנימציית ה-`pop` הקיימת (14px→8px, .35s→.25s). נבדק: `#splash` קיים ב-HTML הגולמי (curl) ומוסר בפועל אחרי הטעינה.
כל שינוי נבדק ידנית + 23/23 בדיקות Playwright עוברות.

**כל 10 הסעיפים (V1-V10) מהתוכנית המקורית הושלמו.** שיפורים עתידיים אפשריים: פרופיל צליל `'tap'` לניווט (דילגתי — ראה הערה ב-V9), עוד ערכות נושא לילדים נוספים, אנימציית "מסלול" למכוש בסולם הכלים.

## מצב קיים (לקרוא לפני שנוגעים)
- **קבצים:** כל העיצוב ב-`styles.css` (~490 שורות, בלי build step). מחלקות עיקריות: `.hero`, `.card`, `.bigbtn`, `.btn`, `.chore-row`, `.bottomnav`, `.modal`, `.streak-banner`, `.kid-card`.
- **ערכת מיינקראפט קיימת:** `.app.minecraft-mode` (styles.css שורות 17-156) מופעלת רק לאריאל ב-`selectChild()` (app.js ~שורה 549). מבוססת על `!important` על כל אלמנט + פונט Press Start 2P.
- **רקע דקורטיבי:** `addMinecraftDecorations()` (app.js ~562) מוסיף `#mc-deco` עם אימוג'י עצים/מכושים סטטיים בשקיפות נמוכה.
- **מצב רגוע (calmMode):** `.app.calm-mode` (styles.css ~468) מכבה אנימציות דקורטיביות ברשימה ידנית; `chime()`/`coinBurst()` מנמיכים עוצמה/חלקיקים.
- **חוקים מחייבים לכל שינוי:** RTL (הכול `direction:rtl` חוץ ממספרים/קוד שמסומנים `direction:ltr`); כל אנימציה דקורטיבית חדשה חייבת להתווסף לרשימת הכיבוי של `.app.calm-mode`; אין להוסיף ספריות חיצוניות (הכול vanilla, אופליין דרך Service Worker — אם מוסיפים קובץ, לעדכן `PRECACHE` + להקפיץ `CACHE_VERSION` ב-sw.js); לבדוק ידנית בפריוויו וגם להריץ `npx playwright test` (23 בדיקות) אחרי כל סעיף.

---

## V1 — כיבוי תנועה מלא: `prefers-reduced-motion` + השלמת calm-mode 🌿 (S)
**מטרה:** מי שהגדיר במכשיר "הפחת תנועה" יקבל אפס אנימציות דקורטיביות אוטומטית — בלי קשר למצב רגוע.
1. ב-`styles.css` להוסיף בסוף:
```css
@media (prefers-reduced-motion: reduce){
  *,*::before,*::after{animation-duration:.01ms !important;animation-iteration-count:1 !important;transition-duration:.01ms !important;}
}
```
2. להרחיב את רשימת `.app.calm-mode` הקיימת כך שתכסה גם אנימציות שנוספו מאוחר: `.learn-dot.active` (transform), `@keyframes bob` על `.big-coin`/`.picker-logo` (כבר כלול), `.scan-line` (פונקציונלי — להשאיר), `#gameTimerChip.warning` (פונקציונלי-בטיחותי — להשאיר).
3. `coinBurst()` ב-app.js: כשמתקיים `matchMedia('(prefers-reduced-motion: reduce)').matches` — לא לירות חלקיקים בכלל (היום רק calmMode מפחית).
**קריטריון קבלה:** הפעלת reduce-motion ב-OS ⇒ המטבע בהירו לא קופץ, פתיחת view בלי אנימציית pop, סימון מטלה עדיין עובד ומציג טוסט.

## V2 — שדרוג ערכת המיינקראפט לבלוקים אמיתיים 🧱 (M)
**מטרה:** היום ה"מיינקראפט" של אריאל הוא בעיקר פונט פיקסלים + פסים עדינים. להפוך אותו למשכנע באמת בעזרת שפת הצורות של המשחק — בלי לגעת בערכת ברירת-המחדל של שאר הילדים.
1. **פינות מרובעות:** בתוך `.app.minecraft-mode` לדרוס `border-radius` ל-4px (לא 0 — נגיעה רכה) על `.card,.btn,.bigbtn,.chore-row,.modal,.kid-card,.game-row`.
2. **מסגרת bevel בסגנון כפתור מיינקראפט:** על `.app.minecraft-mode .btn` ו-`.bigbtn`:
```css
border:3px solid; border-color:rgba(255,255,255,.45) rgba(0,0,0,.35) rgba(0,0,0,.35) rgba(255,255,255,.45);
```
(בהיר למעלה/שמאל, כהה למטה/ימין — זה כל הסוד של כפתור מיינקראפט). ב-`:active` להפוך את הכיוונים.
3. **פס אדמה+דשא בראש ההירו:** ל-`.app.minecraft-mode .hero` להוסיף `border-top:8px solid;` בשכבות: `border-image:linear-gradient(180deg,#52C64B 0 40%,#8B7355 40% 100%) 1;` או פשוט שני `box-shadow: inset 0 4px 0 #52C64B, inset 0 8px 0 #8B7355`.
4. **הערה קריטית — הפונט:** ל-Press Start 2P **אין גליפים בעברית** — טקסט עברי נופל בשקט לפונט ברירת המחדל וזה בסדר (המספרים והאנגלית מקבלים פיקסלים). אסור "לתקן" את זה ע"י פונט אחר בלי בדיקה; אם רוצים כותרות עבריות פיקסליות — לנסות את Google Fonts **"Rubik Pixels"** (יש תמיכה בעברית) *רק* על `.section-title` במצב מיינקראפט, ולבדוק קריאוּת עם המשתמש לפני שמרחיבים.
**קריטריון קבלה:** בפרופיל אריאל הכפתורים נראים כמו כפתורי המשחק (בהיר-כהה), לנועה שום שינוי; צילום מסך לפני/אחרי.

## V3 — רקע חי: שמיים, עננים ועומק במקום אימוג'י סטטי ☁️ (M)
**מטרה:** `#mc-deco` היום = 3 שורות אימוג'י שקופים. להחליף ברקע שנותן תחושת עולם.
1. להחליף את תוכן `addMinecraftDecorations()` (app.js ~562) ב-2-3 עננים פיקסליים מ-SVG inline (מלבנים לבנים מדורגים, `shape-rendering:crispEdges`) שנעים לרוחב לאט מאוד (`animation: cloud-drift 90s linear infinite` — איטי=לא מסיח), ושורת בלוקים דקה (דשא/אדמה, גרדיאנטים חוזרים) בתחתית המסך מאחורי התוכן.
2. כיבוי מלא של תנועת העננים ב-calm-mode וב-reduced-motion (`.app.calm-mode ~ #mc-deco *` לא עובד כי mc-deco מחוץ ל-.app — לכן להוסיף למחלקת ה-body: כש-calm פעיל app.js כבר מוסיף class ל-.app; להוסיף גם `document.body.classList.toggle('calm')` באותו מקום, ולכבות עם `body.calm #mc-deco *{animation:none}`).
3. גרסת לילה: אחרי `state.anchored.sleep_time` (שעת שינה, קיים ב-state) — שמיים כהים + ירח + כוכבים סטטיים במקום עננים. בדיקת השעה כבר קיימת בלוגיקת ה-day-strip (`renderDayStrip`) — להשתמש באותו חישוב, לא לכתוב חדש.
**קריטריון קבלה:** רקע חי בפרופיל אריאל ביום, לילי אחרי שעת השינה, קפוא לגמרי במצב רגוע.

## V4 — מטבע שעף אל הארנק (מיקרו-אינטראקציה מרכזית) 🪙 (M)
**מטרה:** הרגע הכי חשוב באפליקציה — הרווחת מטבע — יקבל משוב פיזי מדויק במקום פיצוץ חלקיקים כללי.
1. פונקציה חדשה `coinFly(fromEl)` ב-app.js: יוצרת `<div>` עגול בסגנון המטבע (radial-gradient זהב קיים ב-`.balance-pill .coin`), ממוקם `position:fixed` על קואורדינטות `fromEl.getBoundingClientRect()`, מונפש ב-Web Animations API (`el.animate(...)`) אל מיקום `#balTop` (הפיל בטופבר), scale קטן + fade, ~700ms, ואז `remove()` ועדכון המספר.
2. לחבר: `markChore` (הכפתור שנלחץ), `mathCheck` (תיבת התשובה), `answerLearningQuestion` (כפתור התשובה — הפרמטר `btnEl` כבר קיים!). כשאין אלמנט מקור (QR, שער) — ליפול ל-`coinBurst()` הקיים.
3. במצב רגוע/reduced-motion: בלי טיסה — רק עדכון המספר עם הבהוב עדין (`transition:color`).
**קריטריון קבלה:** סימון מטלה ⇒ מטבע אחד עף מהשורה אל הפיל למעלה והמאזן מתעדכן בדיוק כשהוא "נוחת".

## V5 — האחדת חגיגות: קונפטי בלי הבהוב 🎉 (S)
**מטרה:** חגיגת תג/פרס עקבית ולא מציפה. היום `coinBurst()` יורה אימוג'י אקראיים שנשארים ב-DOM המון (ראינו עשרות שאריות ⭐🌟 ב-getPageText).
1. לוודא ש-`coinBurst()` מנקה כל חלקיק ב-`animationend`/`setTimeout` (לבדוק דליפה: אחרי 5 שניות `document.querySelectorAll('.burst').length===0`).
2. תקרת חלקיקים בו-זמנית (למשל 12; במצב רגוע 0 — היום זה 5).
3. שום פלאש מסך-מלא/ריצוד: חגיגת התג (מודל) נשארת סטטית עם pop יחיד.
**קריטריון קבלה:** אחרי רצף של 5 זכיות אין שאריות DOM ואין יותר מ-12 חלקיקים בו-זמנית.

## V6 — ערכת נושא לנועה 🦄 (M)
**מטרה:** לאריאל יש עולם; לנועה אין. לתת גם לה זהות — ורוד-פסטל/חד-קרן/כוכבים (לפי `c.color`/`c.emoji` הקיימים אצלה), באותו מנגנון בדיוק.
1. להכליל את `selectChild()`: במקום `if(id==='ariel')` קשיח — שדה `theme` על אובייקט הילד (`state.children[i].theme='minecraft'|'unicorn'|'none'`, ברירת מחדל none; מיגרציה חד-פעמית: `ariel⇒minecraft`, `noa⇒unicorn`). בחירת theme לילד בהגדרות ההורים (לשונית 👧 ילדים, dropdown קטן).
2. `.app.unicorn-mode` ב-styles.css: רקע גרדיאנט ורוד-סגול פסטלי (מהצבעים הקיימים `--pink`,`--purple`), עננים ורודים מעוגלים ב-`#mc-deco` (לעשות את הפונקציה גנרית: `addThemeDecorations(theme)`), כוכבים סטטיים. **בלי** פונט מיוחד.
3. לשמור על אותם חוקי calm/reduced-motion.
**קריטריון קבלה:** מעבר בין הילדים בפיקר מחליף עולם שלם; ילד שלישי חדש מקבל ברירת מחדל נקייה.

## V7 — "מכרה" אמיתי במסך הלמידה + סולם הכלים ⛏️ (M)
**מטרה:** מסך מכרה הידע נראה היום כמו כל מסך. לתת לו אווירת מערה + להשלים את "סולם הכלים" מ-LEARNING-IMPROVEMENTS L5 שלא מומש חזותית.
1. ל-`#view-learn` רקע כרטיס כהה-חום עדין (גרדיאנט `#3E2F23→#5a4632`, טקסט בהיר) רק סביב אזור השאלה, עם 2-3 "עורקי יהלום" — ריבועים טורקיז קטנים ב-`box-shadow` פינתיים.
2. סולם כלים: פונקציה `currentTool(k)` שמחזירה `{emoji,label,next}` לפי סה"כ תשובות נכונות מצטבר (`Object.values(k.learn.correctTotal).reduce(...)`): 0-24 🪵 מכוש עץ, 25-74 🪨 אבן, 75-149 ⚙️ ברזל, 150-299 ✨ זהב, 300+ 💎 יהלום. להציג ב-header של view-learn: הכלי הנוכחי + פס התקדמות לכלי הבא ("עוד 13 תשובות למכוש ברזל!"). תצוגה בלבד — אפס השפעה על מטבעות.
3. מסך הסיכום: הכלי מופיע ליד התוצאה.
**קריטריון קבלה:** ילד עם 30 תשובות נכונות רואה מכוש אבן ופס התקדמות לברזל; הבדיקות הקיימות של הלמידה עדיין עוברות.

## V8 — ניגודיות ונגישות טקסט 🔍 (S)
**מטרה:** כמה צירופים היום גבוליים לילד עם קושי ראייתי/קשב.
1. לבדוק (ידנית עם DevTools או כלי ניגודיות) את: `--gold-d` על `#FFF6E0` (`.kid-card .kc-bal`), `--muted` (#9B96B0) על לבן בטקסטים קטנים מ-.75rem, טקסט `.sub` בתוך `.bigbtn` (לבן על mint בהיר).
2. כל מה שמתחת ליחס 4.5:1 — להכהות את הטקסט (לא לשנות את הרקעים המזוהים עם המותג). למשל `--muted⇒#807A99`.
3. גדלי מגע: לוודא שכל אלמנט לחיץ ≥44px גובה (החשודים: `.icon-btn` 34px, `.chat-speak-btn` 30px — להגדיל את אזור המגע עם padding, לא בהכרח את הנראות).
**קריטריון קבלה:** אף טקסט מידע מתחת ל-4.5:1; כל מטרות המגע במסכי הילד ≥44px.

## V9 — צלילים נעימים ועקביים 🔊 (S)
**מטרה:** היום יש `chime()` יחיד (שני מצבים). מחקר הילדים: צליל שגיאה חד אסור; משוב חיובי מגוון מחזק.
1. להרחיב את `chime()` (Web Audio קיים, בלי קבצים): 3 פרופילים — `success` (עולה, קצר), `celebrate` (ארפג'יו 3 תווים לתג/מפגש מושלם), `tap` (קליק רך לניווט — **כבוי כברירת מחדל**, מופעל רק אם ההורה בחר).
2. טעות: לעולם ללא צליל (היום כבר כך — לוודא שנשאר).
3. calmMode: הכול שקט לגמרי חוץ מ-success בעוצמה מינימלית (התנהגות קיימת — לשמר).
**קריטריון קבלה:** תג חדש נשמע שונה ממטבע בודד; אין שום צליל בטעות; במצב רגוע כמעט דממה.

## V10 — מסך פתיחה (splash) ומעברים 🚪 (S)
**מטרה:** האפליקציה נפתחת היום ישר לתוכן קופץ. פתיח קצר ורגוע נותן צפידות (predictability) שילדים על הרצף אוהבים.
1. `index.html`: div קבוע `#splash` (לוגו המטבע הקיים `.picker-logo` + שם האפליקציה) שמוצג מיידית מה-HTML (בלי JS) ומוסתר ב-fade של 250ms כש-`init()` מסיים (בסוף ה-bootstrap הקיים ב-app.js).
2. תקרה: אם תוך 4 שניות לא הוסתר — להסתיר בכוח (שלא ייתקע על שגיאה).
3. אנימציית ה-view הקיימת (`@keyframes pop`) לרכך: `translateY(14px)⇒8px`, משך `.35s⇒.25s` — פחות "קפיצה", אותה חיות.
**קריטריון קבלה:** פתיחה נראית: לוגו ⇒ fade ⇒ המסך הנכון; אף פעם לא מסך לבן ריק.

---

## סדר מימוש מומלץ
| שלב | סעיפים | רציונל | מאמץ |
|-----|--------|--------|------|
| 1 | **V1, V5, V8** | בטיחות חושית ותיקוני יסוד — לפני כל קישוט | S+S+S |
| 2 | **V4, V9** | המשוב המרכזי של האפליקציה (מטבע+צליל) | M+S |
| 3 | **V2, V3, V7** | העולם של אריאל (מיינקראפט עמוק) | M+M+M |
| 4 | **V6** | העולם של נועה (תלוי בהכללת ה-theme מ-V2/V3) | M |
| 5 | **V10** | ליטוש אחרון | S |

**לכל סעיף:** בדיקה ידנית בפריוויו (פרופיל אריאל + נועה + מצב רגוע), הרצת `npx playwright test`, צילום מסך לפני/אחרי, קומיט נפרד עם הסבר, ו-push (הדיפלוי אוטומטי).
