<?xml version="1.0" encoding="utf-8"?>
<!-- Mirrors ScreenTimeSpike's proven overlay_timer.xml (confirmed working on
     a real device) almost verbatim: same structure, recolored to the app's
     own warning-threshold palette. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/overlayRoot"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="@color/game_timer_green"
    android:paddingStart="16dp"
    android:paddingEnd="8dp"
    android:paddingTop="10dp"
    android:paddingBottom="10dp">

    <TextView
        android:id="@+id/txtOverlayTimer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="00:00"
        android:textColor="@color/white"
        android:textSize="20sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/btnOverlayClose"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:padding="6dp"
        android:text="✕"
        android:textColor="@color/white"
        android:textSize="16sp"
        android:textStyle="bold"
        android:contentDescription="@string/game_time_overlay_close_cd" />

</LinearLayout>
